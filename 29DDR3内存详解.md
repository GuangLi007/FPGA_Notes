---
tags:
  - FPGA
  - DDR3
  - MIG
  - Xilinx
---

## 核心结论

- **DDR3 SDRAM** = 第三代双倍数据率同步动态随机存储器，1.5V，8n 预读取，800~2133 Mbps
- ACX720 板载 **MT41J128M16HA-125**（128M×16bit = 256MB，DDR3-1066）
- Xilinx 7 系列通过 **MIG (Memory Interface Generator)** IP 核驱动 DDR3
- XC7A35T 仅 **HR bank**，DDR3 最大速率 1066 Mbps（533 MHz 时钟）
- FPGA 的 DDR3 控制器 = PHY + 内存控制器 + 用户接口（Native / AXI4）

---

## 一、DDR 发展简史

| 代 | 推出 | VDDQ | 预读取 | 速率 (Mbps) | 关键变化 |
|:--:|:----:|:----:|:------:|:-----------:|----------|
| SDRAM | 1993 | 3.3V | 1n | 66~133 | 单沿采样 |
| DDR | 2000 | 2.5V | 2n | 200~400 | 双沿采样，引入 DQS |
| DDR2 | 2003 | 1.8V | 4n | 400~800 | OCD, ODT |
| **DDR3** | **2007** | **1.5V** | **8n** | **800~2133** | **CWL, ZQ 校准, ASR** |
| DDR4 | 2014 | 1.2V | 8n | 1600~3200 | Bank Group, DBI |
| DDR5 | 2020 | 1.1V | 16n | 3200~6400 | 双 32bit 通道, PMIC |

---

## 二、DDR3 核心架构

### 内部结构

```
                ┌──────────────────────────────────┐
                │           DDR3 芯片内部             │
                │  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  │
Row Decoder ─→│ Bank0 │  │ Bank1 │ ... │ Bank7 │  ← DDR3 有 8 个 Bank
                │  └──┬──┘  └──┬──┘  └─────┘  └─────┘  │
                │     │        │                         │
                └─────┴────────┴─────────────────────────┘
                      │         │
                      └────┬────┘
                    Column MUX & Sense Amp
                           │  (行缓冲 = 8n × 列数)
                     I/O Gating
                           │
                      DQ[15:0]  ← 16bit 数据总线
```

### 8n 预读取

DDR3 的核心：一次列读命令触发 8 个连续数据（BL=8），内部预读取 8n bit，通过 DQS 双沿在 4 个时钟周期内送出。

```
内部(芯片核):  [b0 b1 b2 b3 b4 b5 b6 b7]  ← 8n prefetch
                    ↓
IO 时钟双沿输出:  b0→b1→b2→b3→b4→b5→b6→b7
                  ↑CLK↑   ↑   ↑   ↑   ↑   ↑
```

### 引脚定义

| 类型 | 信号 | 方向 | 说明 |
|:----:|:----:|:----:|------|
| 差分时钟 | CK_P / CK_N | FPGA→DDR | 芯片主时钟，所有操作同步于此 |
| 数据总线 | DQ[15:0] | ↔ | 16bit，每 8bit 为一字节组 |
| 数据选通 | DQS_P/N[1:0] | ↔ | 每个字节组对应一对 DQS，写时 FPGA 输出，读时 DDR 输出 |
| 写掩码 | DM[1:0] | FPGA→DDR | 写时屏蔽对应字节（DM=1 时不写入） |
| Bank 地址 | BA[2:0] | FPGA→DDR | 选择 8 个 Bank 之一 |
| 行/列地址 | A[15:0] | FPGA→DDR | 行/列地址线复用 |
| 片选 | /CS | FPGA→DDR | 低有效 |
| 命令 | /RAS /CAS /WE | FPGA→DDR | 三者组合编码命令 |
| 时钟使能 | CKE | FPGA→DDR | 高有效，低=自刷新/断电 |
| 片内端接 | ODT | FPGA→DDR | 控制 DQ/DQS 端接电阻 |
| 复位 | /RESET | FPGA→DDR | DDR3 专用，低=全部复位 |
| 校准 | ZQ | 240Ω→GND | ZQ 校准参考电阻 |

### 命令集

| 命令 | /CS | /RAS | /CAS | /WE | 功能 |
|:----:|:---:|:----:|:----:|:---:|------|
| DESL | H | X | X | X | 取消选择 |
| NOP | L | H | H | H | 无操作 |
| ACT | L | L | H | H | 行激活：送 Bank+Row 地址 |
| RD | L | H | L | H | 列读：A10=1 → auto-precharge |
| WR | L | H | L | L | 列写：A10=1 → auto-precharge |
| PRE | L | L | H | L | 预充电：A10=1 → 全部 Bank |
| REF | L | L | L | H | 自动刷新 |
| MRS | L | L | L | L | 模式寄存器设置（BA 选择寄存器号） |
| ZQCL | L | H | H | L | ZQ 长校准（上电初始化用） |
| ZQCS | L | H | L | H | ZQ 短校准（运行中周期校准） |

---

## 三、DDR3 时序详解

### 关键时序参数（以 MT41J128M16-125 为例）

|            参数            | 说明               |  DDR3-1066 (CL=7)  | DDR3-800 (CL=6)  |
| :----------------------: | ---------------- | :----------------: | :--------------: |
|           tCK            | 时钟周期             | 1.875 ns (533 MHz) | 2.5 ns (400 MHz) |
|    tCL (CAS Latency)     | 读命令→数据输出         |  7 tCK (13.1 ns)   |  6 tCK (15 ns)   |
|           tRCD           | 行激活→列命令          |  7 tCK (13.1 ns)   |  6 tCK (15 ns)   |
|           tRP            | 预充电时间            |  7 tCK (13.1 ns)   |  6 tCK (15 ns)   |
|           tRAS           | 行激活最小时间          |  20 tCK (37.5 ns)  |  18 tCK (45 ns)  |
|           tRC            | 行循环 (tRAS+tRP)   |  27 tCK (50.6 ns)  |  24 tCK (60 ns)  |
|           tRFC           | 刷新周期             |  110 tCK (206 ns)  | 72 tCK (180 ns)  |
| tCWL (CAS Write Latency) | 写命令→DQ 有效        |       5 tCK        |      5 tCK       |
|           tWTR           | 写后读延迟            |       4 tCK        |      4 tCK       |
|           tWR            | 写恢复时间            |       6 tCK        |      6 tCK       |
|           tFAW           | 四行激活窗口           |  27 tCK (50.6 ns)  |  20 tCK (50 ns)  |
|           tRRD           | 行激活到行激活（不同 Bank） |       4 tCK        |      4 tCK       |

### 读操作时序

```
CLK   ┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐
          │ │ │ │ │ │ │ │ │ │ │ │ │ │ │ │
CMD    ﹉┤ACT├﹉﹉﹉┤RD ├﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉|﹉
          └──┘     └──┘
ADDR   ﹉┤ROW├﹉﹉┤COL├﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉|﹉
          └──┘     └──┘
                   │◄── tCL ──►│
DQS    ﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉╲╱╲╱╲╱╲╱╲╱﹉﹉
                               └── BL8 数据 (4 CLK) ──┘
DQ     ﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉┤D0┤D1┤D2┤D3┤D4┤D5┤D6┤D7├﹉
                              └── DDR 双沿 ──┘
          ◄── tRCD (7 tCK) ──►
```

### 写操作时序

```
CLK   ┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┐┐┌┐┌┐┌┐
          │ │ │ │ │ │ │ │ │ │ │ │││ │ │ │
CMD    ﹉┤ACT├﹉﹉┤WR ├﹉﹉﹉﹉﹉﹉﹉|﹉
          └──┘    └──┘
ADDR   ﹉┤ROW├﹉┤COL├﹉﹉﹉﹉﹉﹉﹉|﹉
          └──┘    └──┘
                  │◄tCWL►│
DQS    ﹉﹉﹉﹉﹉﹉﹉﹉╲╱╲╱╲╱╲╱╲╱﹉﹉﹉
                        └── FPGA 输出 DQS ──┘
DQ     ﹉﹉﹉﹉﹉﹉﹉﹉┤D0┤D1┤...┤D7├﹉﹉﹉
                  │◄ tWPST ►│
```

### Bank 管理

DDR3 有 8 个 Bank，Bank 管理是提高带宽的关键：

```
关键原则:
  1. 一个 Bank 在同一时间只能有**一行**激活
  2. 不同 Bank 可独立激活不同行（并行）
  3. 在 Bank A 读数据期间，可提前激活 Bank B 的下一行
  
Bank 流水示例:
  t0: ACT Bank0-RowX
  t7: ACT Bank1-RowY + RD Bank0-ColA  ← 同时发出
  t14: RD Bank1-ColB + PRE Bank0      ← 读 Bank0 的同时预充电
  t15: ACT Bank0-RowZ                  ← 激活新行
```

Bank 管理由 MIG 自动处理，用户只需发地址（行地址自动关联 Bank）。

---

## 四、DDR3 初始化与模式寄存器

### 初始化序列

```
1. 上电 → CKE 保持低 ≥ 500 μs（等待电源稳定）
2. CKE = H，等待 tXPR ≥ 400 ns
3. NOP 持续至少 200 μs
4. MRS: MR2 → 配置 CWL、写恢复、突发长度
   - BA[2:0] = 010, A[15:0] 见下
5. MRS: MR3 → 温度控制
   - BA[2:0] = 011, A[2] = 1 启用 ASR
6. MRS: MR1 → ODT、驱动强度、附加延迟（AL）
   - BA[2:0] = 001
7. MRS: MR0 → CAS 延迟、突发类型/长度
   - BA[2:0] = 000
8. ZQCL: ZQ 长校准 (tZQinit = 512 tCK)
9. 等待校准完成 → 进入正常工作状态
```

### MR0 — 基本配置 (BA=0)

| 位域 | 字段 | 说明 |
|:----:|:----:|------|
| A[2:0] | 突发长度 | 010=BL8, 000=BC4（固定）|
| A3 | 读突发类型 | 0=Sequential, 1=Interleave |
| A[6:4] | CAS 延迟（CL） | DDR3-1066: 0110=CL6, 0111=CL7 |
| A7 | 测试模式 | 0=正常, 1=厂商测试 |
| A[8] | DLL 复位 | 1=DLL 复位 |
| A[11:9] | 写恢复（WR） | 010=WR5, 011=WR6 |
| A[12] | DQS# 使能 | 1=差分 DQS 使能 |

### MR1 — 驱动与端接 (BA=1)

| 位域 | 字段 | 说明 |
|:----:|:----:|------|
| A[2:0] | 输出驱动强度 | 010=RZQ/6(40Ω), 011=RZQ/7(34Ω) |
| A[5:4] | ODT | 01=60Ω(RZQ/4), 10=120Ω(RZQ/2) |
| A[6] | 附加延迟(AL) | 0=0, 1=CL-1 |
| A[7] | 写均衡使能 | 1=启用 |
| A[9] | TDQS | 0=禁用 |
| A[12] | 输出缓冲使能 | 0=使能 |

### MR2 — 时序配置 (BA=2)

| 位域 | 字段 | 说明 |
|:----:|:----:|------|
| A[2:0] | CWL | DDR3-1066: 010=CWL5, 011=CWL6 |
| A[5:4] | 写恢复自动预充电 | 00=动态, 10=固定 |
| A[6] | 自刷新温度范围 | 0=正常, 1=扩展 |
| A[7] | 动态 ODT | 0=禁用 |

### MR3 — 温度控制 (BA=3)

| 位域 | 字段 | 说明 |
|:----:|:----:|------|
| A[2] | ASR | 1=自刷新温度范围自动调整 |
| A[3] | SRT | 0=正常范围, 1=扩展范围 |

---

## 五、DDR3 高级特性

### ZQ 校准

ZQ 校准补偿 PVT（工艺-电压-温度）变化，确保输出驱动和 ODT 精确：

```
                    VDDQ
                     │
                    RZQ (240Ω)
                     │
              ┌──────┤
              │      │
芯片内部      │   比较器 ← 参考电压
              │      │
              └──────┘
              ┌──────┐
              │ 校准逻辑 │ → 调整驱动/ODT
              └──────┘
```

- **ZQCL**：长校准，上电初始化时执行，耗时 512 tCK
- **ZQCS**：短校准，运行中周期执行（建议每 50ms 一次），耗时 64 tCK

### ODT（片内端接）

ODT 在芯片内部提供端接电阻，消除信号反射：

```
写操作（FPGA→DDR）: DDR 芯片启用 ODT，匹配传输线阻抗
读操作（DDR→FPGA）: FPGA 端端接，DDR 输出驱动
```

| ODT 设置 | 阻值 | 适用场景 |
|:--------:|:----:|----------|
| RZQ/4 | 60Ω | DDR3 最常用，匹配 50Ω~60Ω 走线 |
| RZQ/2 | 120Ω | 轻负载/点对点 |
| 高阻 | ∞ | 关闭 ODT |

### 写均衡 (Write Leveling)

补偿 CK 与 DQS 的走线长度差异。DDR3 在初始化时执行：

```
1. DDR3 采样 DQS 边沿相对于 CK 的位置
2. FPGA 调整 DQS 延迟使 DQS 边沿对齐 CK 上升沿
3. 通过 MR1[7] 启用
```

### 自刷新与断电

| 模式 | CKE | CK | 说明 |
|:----:|:---:|:--:|------|
| 正常 | H | 运行 | 正常读写 |
| 自刷新 | H→L | X | 芯片内部保持刷新，低功耗 |
| 断电 | L | 停止 | CK 停止，快速唤醒 |
| 深度断电 | L | 停止 | /RESET=L，最省电，数据丢失 |

---

## 六、地址映射

### DDR3 地址组成 (MT41J128M16)

| 字段 | 宽度 | 说明 |
|:----:|:----:|------|
| Rank | 0 | 单 Rank（DDR3 芯片无 Rank 概念，DIMM 才有） |
| Bank | 3 bit (BA[2:0]) | 8 个 Bank |
| Row | 14 bit (A[13:0]) | 16384 行 |
| Column | 10 bit (A[9:0]) | 1024 列 |

总容量计算：
```
= Banks × Rows × Columns × DQ 位宽 × BL
= 8 × 16384 × 1024 × 16 × 8/8  ← 8n 预读取自动输出 8 个
注: JEDEC 的标准容量计算不考虑 BL 因子，因 Prefetch 是内部行为
  
标准计算:
  8 × 16384 × 1024 × 16 bit = 2,147,483,648 bit = 2 Gbit = 256 MB
```

### MIG 用户地址映射

MIG 将用户地址映射为 {Bank, Row, Column}：

```
用户地址[28:0] = {Bank[2:0], Row[13:0], Column[9:0]} （默认配置）

用户地址递增 1 → 下一个 Column（burst 内部顺序递增）
用户地址跨越 Column 边界 → 自动跨到同一行的下一列
用户地址跨越 Row 边界 → 需要新 ACT（MIG 自动管理）
```

---

## 七、Xilinx 7 系列 + MIG IP

### MIG 三层架构

```
FPGA Fabric
┌───────────────────────────────────────────────┐
│   User Interface (Native / AXI4)               │
│  ┌──────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ 写 FIFO│→│ 命令队列  │→│ Bank 管理 + 调度  │  │
│  │ 读 FIFO│←│          │  │ (Bank轮询/刷新)   │  │
│  └──────┘  └──────────┘  └────────┬─────────┘  │
│     物理层 PHY                    │            │
│  ┌──────────────────────────────────┐          │
│  │ 写路径: DQ/DQS/数据输出 + WDQS   │          │
│  │ 读路径: DQS 门控 + IDELAY + 采样 │          │
│  │ 校准: DQS 相位 + Read Training   │          │
│  └──────────────┬───────────────────┘          │
└─────────────────┼──────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │    FPGA IOB       │
        │  (HR bank)        │
        │  1.5V SSTL Class I│
        └─────────┬─────────┘
                  │
        ┌─────────┴─────────┐
        │  MT41J128M16      │
        │  DDR3 SDRAM       │
        └───────────────────┘
```

### XC7A35T + HR Bank 限制

| 参数 | 值 |
|:----:|:---:|
| 最大时钟 | 533 MHz (DDR3-1066) |
| DQ 宽度 | 最大 16bit |
| IOSTANDARD | SSTL135 / SSTL15 Class I |
| 不支持 | DDR4、DDR3-1866+、HP bank 需要 |

### MIG 生成配置要点

```
Board:  ACX720, DDR3 256MB, 16bit
Memory Part: MT41J128M16XX-125
Speed Grade: -2 (XC7A35T-2FGG484C)

Clock Period: 3000 ps (DDR3-667) — 保守稳定
              1875 ps (DDR3-1066) — XC7A35T 上限

Input Clock Period: 20000 ps (50 MHz)
System Clock:  Single-Ended
Reference Clock: 200 MHz (由 MMCM 从 50MHz 生成)

Memory Voltage: 1.5V
Data Width: 16
Data Mask: Enable
Burst Length: 8

Output Driver: RZQ/7 (34Ω)
ODT: RZQ/4 (60Ω)
CAS Latency: 取决于速率和 Speed Grade
```

### MIG Native 接口

```verilog
// 写操作 (Native 接口)
assign app_cmd     = 3'b000;               // 写
assign app_addr    = {bank, row, col};
assign app_en      = write_en;
// app_rdy = 1 时命令被接收

// 写数据（支持命令与数据错拍）
assign app_wdf_data    = wdata;            // 64bit (2×32 = 2×DQ_位宽)
assign app_wdf_wren    = write_en;
assign app_wdf_end     = write_en;         // 单拍写完成则=wren
// app_wdf_rdy = 1 时数据被接收

// 读操作
assign app_cmd     = 3'b001;               // 读
assign app_addr    = {bank, row, col};
assign app_en      = read_en;
// app_rd_data_valid = 1 时读数据有效
```

### 读写时序

```
写:                                    读:
  clk  ┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐              clk  ┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐
  cmd  ╲_/WR/____/﹨                    cmd  ╲_/RD/____/﹨
  addr ╲A/_____/﹨                      addr ╲A/_____/﹨
  data _____╲_____/D/﹨                  q    ________/Q/﹨
  app_rdy ___/﹨___/﹨___                app_rdy ___/﹨___/﹨
  app_wdf_rdy  _/﹨___/﹨                rd_valid _______/﹨___
```

---

## 八、DDR3 调试指南

### 正常启动标志

```
MIG 输出 init_calib_complete = 1 → DDR3 初始化成功，可正常读写
如果一直为 0，说明 PHY 校准失败
```

### 使用 ILA 调试步骤

```
1. 添加 ILA 到 MIG 用户接口（app_rdy, app_en, app_cmd 等）
2. 写固定 Pattern: 连续写 0~255 递增数据
3. 读回比较: 连续读同一地址范围
4. 检查:
   - app_rdy 是否握手成功
   - app_rd_data_valid 是否出现
   - 读回数据是否 = 写入数据
```

### 基准测试

```verilog
// 简单读写测试 - 写满→读回比较
reg [9:0]  burst_cnt;
reg [28:0] test_addr;
reg        test_fail;

always @(posedge clk) begin
    if (init_calib_complete) begin
        // 写阶段
        if (test_addr < END_ADDR && app_rdy) begin
            test_addr <= test_addr + 1;
        end
        // 读阶段（写完后发读命令）
        // ...
        // 比较
        if (app_rd_data_valid) begin
            if (app_rd_data != expected_data) begin
                test_fail <= 1'b1;  // 失败标志
            end
        end
    end
end
```

### 常见问题

| 现象 | 原因 | 解决 |
|:----|------|------|
| init_calib_complete 一直为 0 | PHY 校准失败 | 检查引脚约束、DDR 电源、时钟频率 |
| 写 OK 读错位 | DQS 相位调整不当 | MIG 中调整 Read DQS 相位偏移 |
| 温度升高后出错 | IDELAY 漂移 | 开启 MIG 温度补偿或周期重校准 |
| 隔一段时间挂死 | 刷新冲突 | 检查 tRFC 配置是否正确 |
| DQS 门控训练失败 | DQS 偏出校准范围 | 减少 PCB 走线长度差，降低时钟频率 |

---

## 九、DDR4 / DDR5 对比（参考）

### DDR4 vs DDR3

| 特性 | DDR3 | DDR4 |
|:----:|:----:|:----:|
| 供电 | 1.5V | 1.2V |
| Bank | 8 个 Bank | 4 个 Bank Group × 4 Bank = 16 Bank |
| DBI | 无 | 有（降低功耗） |
| CRC | 无 | 写 CRC |
| CA 奇偶 | 无 | 有（命令地址奇偶校验） |
| VREF | 全局 VREF | VREFDQ 内部生成 |
| ODT | 静态控制 | 动态 ODT |
| 最大单芯片 | 8Gb | 16Gb |
| 7 系列支持 | ✅ MIG | ❌ 不支持 |

### DDR5 vs DDR4

| 特性 | DDR4 | DDR5 |
|:----:|:----:|:----:|
| VDDQ | 1.2V | 1.1V |
| 预读取 | 8n | 16n |
| DIMM 通道 | 64bit × 1 | 32bit × 2 |
| 速率 | 1600~3200 | 3200~6400 |
| PMIC | 主板供电 | DIMM 自带 PMIC |
| SPD | 简单 EEPROM | SPD Hub (I²C) |
| ECC | 可选 | On-Die ECC |

---

## 十、DDR3 vs FPGA 内部存储

| 特性 | Block RAM (BRAM) | Distributed RAM | DDR3 (外部) |
|:----:|:----------------:|:---------------:|:-----------:|
| 容量 | 36 Kb/块 | ~64bit/LUT | 256 MB+ |
| 延迟 | 1~3 clk | 1 clk | 20~50 clk |
| 带宽 | ~10 GB/s | ~50 GB/s | ~2 GB/s (DDR3-800 16bit) |
| 资源消耗 | 专用 BRAM | 逻辑资源 | IO + PHY + 控制器 |
| 适合 | FIFO、小缓存 | 寄存器文件 | 帧缓存、大缓冲区 |

典型场景：**HDMI 帧缓冲区**（[[24HDMI 显示驱动详解]]）需要 DDR3 存储一帧图像（1280×720×24bit ≈ 2.7MB），BRAM 远不够用。

---

## 参考

- JEDEC JESD79-3F: DDR3 SDRAM Specification
- Xilinx UG586: 7 Series MIG User Guide
- Xilinx UG471: 7 Series SelectIO Resources
- MT41J128M16HA-125 数据手册（Micron）
- ACX720 原理图 & 管脚分配表
- [[24HDMI 显示驱动详解]] — DDR3 帧缓冲应用
- [[21FPGA FIFO 详解]] — DDR3 与 FIFO 配合
- [[30AXI4总线协议详解]] — AXI4 作为 DDR3 用户接口
