# 按键消抖三种方法设计与对比

## 为什么需要消抖

机械按键按下/松开的瞬间，金属触点会产生 **弹跳（bounce）**，持续 5~20ms：

```
理想： ▁▁▁▁▁▁▁╲▁▁▁▁▁▁▁▁▁▁▁  （一次跳变）
实际： ▁▁▁▁╱╲╱╲╱╲╱╲╱╲▁▁▁▁  （抖动）
```

不消抖 → 一次按键被识别为多次输入。

> ACX720 按键按下为 **低电平**（0），释放为 **高电平**（1，上拉）。

## 工程结构

```
Key_Debounce/
├── src/hdl/
│   ├── debounce_delay.v         # 方法①：延时采样法
│   ├── debounce_continuous.v    # 方法②：连续采样法
│   ├── debounce_fsm.v           # 方法③：状态机法
│   └── key_debounce_top.v       # 顶层（实例化 debounce_delay）
└── src/constraints/
    └── key_debounce.xdc
```

顶层实现：按下 S1 → LED0 翻转（每按一次切换亮灭）。

---

## 方法①：延时采样法（`debounce_delay`）

### 原理

1. 一个**自由运行**的计数器持续递增
2. 每当计数器溢出（`cnt == T_20MS - 1`），**采样**一次同步后的按键值
3. 对采样值做**边沿检测**，下降沿 → `key_press` 脉冲

等效于每隔 20ms"看一眼"按键，如果看到从 1→0 变化，就认为按下了。

### 关键代码结构

```
key_in → [2级同步器] → [每20ms采样] → [边沿检测] → key_press
```

- 计数器自由跑，永不复位（最简实现）
- `key_sampled` 每 20ms 更新一次
- `key_sampled_r1` 延迟一拍做边沿比较

### 优缺点

| 优点 | 缺点 |
|------|------|
| 代码最简单 | 响应延迟 0~20ms（平均 10ms） |
| 资源最少（1个计数器+2个触发器） | 抗干扰一般（只采一次） |
| 无复杂逻辑 | — |

---

## 方法②：连续采样法（`debounce_continuous`）

### 原理

1. 以 **1ms 间隔**连续采样按键值，存入 **20 位移位寄存器**
2. 检查移位寄存器中所有位是否**全部相同**
3. 如果全部相同 → 输出该稳定值
4. 对稳定值做边沿检测

等效于"连续看 20 次，如果 20 次结果都一样，才相信是真的"。

### 关键代码结构

```
key_in → [同步] → [每1ms推入移位寄存器] → [全相等判断] → [边沿检测] → key_press
```

使用 `for` 循环遍历比较：`for (i = 1; i < CNT_NUM; i = i + 1)`

### 优缺点

| 优点 | 缺点 |
|------|------|
| 抗干扰最强（连续判断） | 资源稍多（20位移位寄存器） |
| 稳定值无毛刺 | 响应延迟固定 20ms |
| 可通过调节 CNT_NUM 灵活配置 | 组合逻辑比较可能随 CNT 增大变慢 |

---

## 方法③：状态机法（`debounce_fsm`）

### 原理

四状态状态机，**事件驱动**：

```
IDLE → (检测到下降沿) → 立即输出脉冲 → DEBOUNCE → (等待20ms) → PRESSED → (检测到上升沿) → RELEASE → (等待20ms) → IDLE
```

- 在 IDLE 中检测到下降沿**立即**输出 `key_press`（最快响应）
- 进入 DEBOUNCE 后忽略所有电平变化，等待 20ms 消抖窗口
- 在 PRESSED 中等待按键释放
- 进入 RELEASE 再次等待 20ms 消抖

### 关键代码结构

```verilog
key_fall =  key_sync2 & ~key_sync1;
key_rise = ~key_sync2 &  key_sync1;

IDLE:     key_fall → key_press=1, state=DEBOUNCE
DEBOUNCE: cnt==20ms → state=PRESSED
PRESSED:  key_rise → state=RELEASE
RELEASE:  cnt==20ms → state=IDLE
```

### 优缺点

| 优点 | 缺点 |
|------|------|
| **响应最快**（边沿即输出，0 延迟） | 代码最复杂 |
| 释放也有消抖（防误判） | 状态机需要仔细设计 |
| 抗噪声能力中等 | — |

---

## 三种方法对比总结

| 特性 | 延时采样法 | 连续采样法 | 状态机法 |
|------|-----------|-----------|---------|
| 代码复杂度 | ⭐ 最简单 | ⭐⭐ 中等 | ⭐⭐⭐ 最复杂 |
| 资源占用 | 最少 | 中等 | 中等 |
| 响应延迟 | 0~20ms（平均10ms） | 固定 20ms | **最小（~0）** |
| 抗干扰能力 | 一般 | **最强** | 中等 |
| 适用场景 | 普通按键 | 噪声大的环境 | 需要快速响应的场合 |

## 板级验证结果

- 项目：`Key_Debounce`
- 烧录方式：Vivado TCL batch mode
- 绑定引脚：S1(A20) → 按键输入，LED0(M22) → 翻转输出
- 验证方法：按 S1 观察 LED0 是否干净切换（无抖动多次翻转）
- 效果：LED0 每按一次稳定翻转一次，无抖动

## 更改 Top 模块使用其他方法

只需修改 `key_debounce_top.v` 中的例化模块名和端口即可切换：

```verilog
// 方法①
debounce_delay u_debounce (...);

// 方法②
debounce_continuous u_debounce (...);

// 方法③
debounce_fsm u_debounce (.key_press(key_press));
// 注：fsm 模块 key_press 为 reg 类型，接 wire 需注意
```

## 相关文件

- 工程路径：`/home/kl/My_Project/FPGA/Key_Debounce/`
- 笔记路径：`/home/kl/Note/Obsidian_Note/FPGA/10按键消抖三种方法设计与对比.md`
