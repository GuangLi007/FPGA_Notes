---
tags:
  - FPGA
---

## 核心结论

**约束文件只定义引脚位置和电平标准，不定义方向。方向由 Verilog 代码中的端口声明决定。**

---

## 一、方向的判断方法

### 1. 看代码中的端口声明

```verilog
module top(
    input clkin,      // ← input = 输入
    output beep       // ← output = 输出
);
```

### 2. 看代码中的使用位置

| 出现位置 | 方向 |
|---------|------|
| `assign` 或 `always` 块的**左边** | 输出 |
| `assign` 或 `always` 块的**右边** | 输入 |
| `if`、`case` 等条件中 | 输入 |

### 3. 看综合/DRC 报错

```verilog
assign clkin = 1'b0;  // 错误！input 不能赋值
```

报错信息：
```
ERROR: [DRC 23-20] Input port clkin is connected to an output buffer
```
这是在开发板的情况下，vivado会检查。
自己设计的板子，不会分析报错。

### 4.物理连接匹配

---

## 二、约束文件 vs 代码文件

| 文件类型 | 定义内容 |
|---------|---------|
| **约束文件 (.xdc)** | 引脚位置 (PACKAGE_PIN)、电平标准 (IOSTANDARD)、时钟周期 |
| **Verilog 文件 (.v)** | 信号方向 (input/output)、逻辑功能 |

**仅看约束文件无法判断方向。**

---

## 三、不同场景的注意事项

### 现成开发板
- 引脚功能基本固定，必须按原理图声明方向
- 晶振引脚只能是 `input`

### 自己设计的板子
- 引脚功能由你的电路决定
- 代码声明必须与物理连接匹配
- Vivado 不会检测外部电路，责任在设计者

---

## 四、晶振引脚的方向

| 晶振类型 | FPGA 引脚方向 |
|---------|--------------|
| 有源晶振 (4脚) | **输入**（晶振输出时钟） |
| 无源晶振 (2脚) | **输入** (XI) + **输出** (XO，反馈) |

**结论：连接到晶振的引脚，在电路上一定是输入（或反馈输出），不能随意改为普通输出。**

---

## 五、常见错误

### 错误1：输入引脚被赋值

```verilog
assign clkin = 1'b0;  // ❌ 错误
```

### 错误2：输出引脚连接到多个驱动源

```verilog
assign beep = signal1;
assign beep = signal2;  // ❌ 多个驱动冲突
```

### 错误3：代码声明与物理电路不匹配

| 代码声明 | 物理连接 | 结果 |
|---------|---------|------|
| `input` | 悬空 | ⚠️ 逻辑不定 |
| `output` | 晶振输出 | ⚠️ 两个输出短接，可能烧毁芯片 |

---

## 六、快速判断口诀

| 信号特征 | 方向 |
|---------|------|
| 被 `always` 或 `assign` 赋值 | 输出 |
| 出现在 `if`、`case` 条件中 | 输入 |
| 被其他模块实例化时连接 | 看子模块定义 |

---

## 七、总结

1. **方向由代码决定，不由约束文件决定**
2. **约束文件只负责映射引脚位置**
3. **电路设计者必须确保代码声明与物理连接一致**
4. **晶振引脚通常是输入，不能随意改为输出**
