# 线性序列机原理与应用

## 概述

线性序列机（Linear Sequence Machine）是一种用**计数器作为"尺子"**的时序控制方法。不同于传统 FSM 的状态寄存器 + case 分支，线性序列机直接利用计数器的值来判断"当前该做什么"——**计数器值就是状态**。

### 核心思想

```
计数器 = 一把尺子
在尺子的不同刻度上，做不同的事情
```

| 特性 | 线性序列机 | 传统 FSM |
|------|-----------|---------|
| 状态表示 | 计数器值 | 独立状态寄存器 |
| 切换方式 | 自动递增 | 条件跳转 |
| 代码结构 | if-else 级联 | case 分支 |
| 适用场景 | 线性、定时控制 | 复杂分支、回环 |
| 资源消耗 | 更少 | 较多 |

---

## 基本结构

### 框架模板

```verilog
reg [N:0] Count;  // 计数器 = 状态机

always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        Count <= 0;
        输出信号 <= 默认值;
    end
    else if (Count < 阶段1_结束值) begin
        // 阶段1: 做事情A
        Count <= Count + 1;
    end
    else if (Count < 阶段2_结束值) begin
        // 阶段2: 做事情B
        Count <= Count + 1;
    end
    else begin
        Count <= 0;  // 循环回起点
    end
end
```

### 数据流

```
复位 → Count=0
        │
        ▼
  ┌─→ Count < N1 → 做事情A → Count++
  │        │
  │        ▼
  │   Count < N2 → 做事情B → Count++
  │        │
  │        ▼
  └── Count=0 ←── 溢出重置
```

---

## 实例分析：LED 亮 0.25s 灭 0.75s

### 需求

- 50MHz 时钟，周期 20ns
- LED 亮 0.25s = 12,500,000 个时钟周期
- LED 灭 0.75s = 37,500,000 个时钟周期
- 总周期 = 1s = 50,000,000 个时钟周期

### 代码

```verilog
module Top(
    input clk,
    input rst_n,
    output reg led
);

reg [25:0] Count;

always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        Count <= 0;
        led <= 0;
    end

    else if (Count < 12_500_000-1) begin   // 阶段1: 亮
        led <= 1;
        Count <= Count + 1;
    end 

    else if (Count < 50_000_000) begin     // 阶段2: 灭
        led <= 0;
        Count <= Count + 1;
    end

    else begin                              // 重置
        Count <= 0;
    end 
end

endmodule
```

### 时序图

```
Count:  0 ──────────────────→ 12_499_999 ──────────────────→ 49_999_999 → 0
led:    1 ←───── 亮0.25s ────→ 0 ←──────── 灭0.75s ────────→ 1 (循环)
         ↑                     ↑                              ↑
      rst_n释放           阶段切换                        周期结束
```

### 计数器分段表

| 阶段 | Count 范围 | 持续周期 | 时间 | led |
|------|-----------|---------|------|-----|
| 亮 | 0 ~ 12,499,999 | 12,500,000 | 0.25s | 1 |
| 灭 | 12,500,000 ~ 49,999,999 | 37,500,000 | 0.75s | 0 |
| 重置 | → 0 | 1 | - | - |

---

## 关键要点

### 1. 计数器 = 尺子

```verilog
// 尺子思维：在刻度上标记事件
// 0         12_500_000        50_000_000
// |───── 亮 ─────|────── 灭 ───────|
```

不需要状态寄存器，**Count 的范围本身就是状态**。

### 2. 减 1 问题

```verilog
// ⚠️ 注意边界！

// 写法A: Count < 12_500_000-1
// Count 到达 12_499_999 时还在阶段1
// Count 到达 12_500_000 时进入阶段2
// 实际亮了 12_500_000 个周期 ✓

// 写法B: Count < 12_500_000  
// Count 到达 12_500_000 时才进入阶段2
// 实际亮了 12_500_001 个周期 ✗ 多了1个
```

**原则**：阶段切换点 = `Count == 阶段结束值 - 1` 时的下一个时钟沿。

### 3. 任意时刻操作

```verilog
// 在计数器的任意刻度都可以做事
else if (Count < 25_000_000) begin
    led <= 1;          // 做事
    buzzer <= 0;       // 同时做另一件事
    Count <= Count + 1;
end
```

### 4. 复位必须完整

```verilog
// ❌ 错误：Count 没复位
if (!rst_n) begin
    led <= 0;
    // Count 没清零 → 不确定状态
end

// ✓ 正确
if (!rst_n) begin
    Count <= 0;
    led <= 0;
end
```

---

## 与传统 FSM 对比

### 同一功能的 FSM 写法

```verilog
parameter LED_ON = 0, LED_OFF = 1;
reg state;
reg [24:0] cnt;

always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        state <= LED_ON;
        cnt <= 0;
        led <= 1;
    end
    else case (state)
        LED_ON: begin
            led <= 1;
            cnt <= cnt + 1;
            if (cnt == 12_499_999) begin
                state <= LED_OFF;
                cnt <= 0;
            end
        end
        LED_OFF: begin
            led <= 0;
            cnt <= cnt + 1;
            if (cnt == 37_499_999) begin
                state <= LED_ON;
                cnt <= 0;
            end
        end
    endcase
end
```

### 对比总结

| 对比项 | 线性序列机 | 传统 FSM |
|--------|-----------|---------|
| 代码行数 | ~15 行 | ~25 行 |
| 寄存器 | 1 个 Count | 1 个 state + 1 个 cnt |
| 可读性 | 刻度直观 | 状态名直观 |
| 扩展性 | 加 `else if` 即可 | 加状态 + case 分支 |
| 回环跳转 | 不方便（只能顺序） | 方便（任意跳转） |
| 适用场景 | 线性定时 | 复杂状态流 |

---

## 应用场景

### 适合线性序列机

| 场景 | 说明 |
|------|------|
| LED 呼吸灯 | 渐亮渐灭，线性 PWM |
| 流水灯 | 按顺序依次点亮 |
| 波形生成 | SPI/I2C 时序，按步骤输出 |
| 延时控制 | 上电延时、复位延时 |
| 简单通信时序 | UART 发送、按键消抖 |

### 适合传统 FSM

| 场景 | 说明 |
|------|------|
| UART 接收 | 需要检测起始位后跳转 |
| 协议解析 | 帧头→数据→校验，可能出错回 IDLE |
| 复杂控制流 | 有分支、有条件回退 |
| 多事件触发 | 外部事件决定下一步 |

---

## 扩展：多阶段线性序列机

### 4 阶段流水灯

```verilog
module Led_Flow(
    input clk,
    input rst_n,
    output reg [3:0] led
);

reg [25:0] Count;

always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        Count <= 0;
        led <= 4'b0001;
    end
    else if (Count < 25_000_000 - 1) begin      // 阶段1
        led <= 4'b0001;
        Count <= Count + 1;
    end
    else if (Count < 50_000_000 - 1) begin      // 阶段2
        led <= 4'b0010;
        Count <= Count + 1;
    end
    else if (Count < 75_000_000 - 1) begin      // 阶段3
        led <= 4'b0100;
        Count <= Count + 1;
    end
    else if (Count < 100_000_000 - 1) begin     // 阶段4
        led <= 4'b1000;
        Count <= Count + 1;
    end
    else begin
        Count <= 0;
    end
end

endmodule
```

### 时序图

```
Count:   0 ────→ 25M ────→ 50M ────→ 75M ────→ 100M → 0
led:    0001   → 0010   → 0100   → 1000   → 0001 (循环)
         ↑       ↑        ↑        ↑
       LED0亮  LED1亮   LED2亮   LED3亮
```

---

## 调试心得

### 问题 1：LED 闪烁不均匀

- **现象**：亮的时间明显比预期长
- **原因**：减 1 写错，`Count < 12_500_000` 比 `Count < 12_500_000-1` 多一个周期
- **解决**：明确阶段边界，画出刻度图验证

### 问题 2：复位后 LED 不亮

- **现象**：按复位键后 LED 全灭
- **原因**：复位块里只清了 `led` 没清 `Count`，或者 `led` 复位值为 0 且第一阶段也是 0
- **解决**：检查复位块和第一阶段的 led 值

### 问题 3：计数器位宽不够

```verilog
// 50_000_000 需要 26 位 (2^26 = 67_108_864 > 50_000_000)
reg [25:0] Count;  // ✓ 26 位
reg [24:0] Count;  // ✗ 2^25 = 33_554_432 < 50_000_000 会溢出
```

**公式**：位宽 ≥ ceil(log2(最大计数值))

---

## 工程文件

| 文件 | 路径 |
|------|------|
| 源码 | `/home/kl/My_Project/FPGA/Line_Ques_1/src/hdl/Top.v` |
| 约束 | `/home/kl/My_Project/FPGA/Line_Ques_1/src/constraints/Cons.xdc` |
| 比特流 | `/home/kl/My_Project/FPGA/Line_Ques_1/Line_Ques_1/Line_Ques_1.runs/impl_1/Top.bit` |

### 引脚分配（ACX720）

| 端口 | FPGA 引脚 | 说明 |
|------|-----------|------|
| clk | Y18 | 50MHz 时钟 |
| rst_n | F15 | 按键 S0，按下复位 |
| led | M22 | LED0，高电平点亮 |
