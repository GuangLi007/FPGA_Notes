---
tags:
  - FPGA
  - 串口屏
  - UART
  - 显示
  - 淘晶驰
---

## 核心结论

- **串口屏** = 自带控制器的 LCD 模组，通过 UART 接收指令来显示内容
- FPGA **不需要实现显示时序**，只需按协议格式发送 UART 数据包
- 常用品牌：淘晶驰（TJC）、Nextion、迪文（DWIN）
- 相比 VGA/HDMI：硬件连线简单（2 线），但刷新率受波特率限制
- 适合：低成本、低分辨率、交互简单的显示场景

---

## 一、串口屏 vs 直驱显示

| 特性 | VGA / HDMI（直驱） | 串口屏 |
|------|:-------------------:|:------:|
| 原理 | FPGA 生成像素时钟 + 同步信号 + RGB | 自带 MCU 解析 UART 指令绘制 |
| 硬件连线 | 多线（RGB×N + HSync + VSync + CLK） | 2 线（TX + RX） |
| 分辨率 | 由 FPGA 时序决定，最高 4K | 由屏幕模组决定，通常 480×272 ~ 1024×600 |
| 刷新率 | FPGA 控制，60Hz+ | 模组控制，受波特率限制（通常 15~30fps） |
| 颜色深度 | 8~16bit/通道 | 通常 16bit（RGB565） |
| 显示内容 | FPGA 逐像素生成 | FPGA 发命令，屏幕自己画 |
| 灵活性 | 极高（可输出任意像素） | 低（只能发预设指令） |
| 开发难度 | 高（TMDS/时序/串行化） | 低（仅需 UART 发送） |
| 功耗 | 较高 | 较低（屏幕自带低功耗管理） |
| 典型场景 | 高帧率视频、游戏画面 | 仪表盘、菜单界面、数据显示 |

### 选型建议

| 需求 | 推荐方案 |
|------|---------|
| 高帧率视频、图像处理 | HDMI（直驱） |
| 简单数据显示、菜单交互 | 串口屏 |
| 原型验证、快速开发 | 串口屏 |
| 低成本、低引脚数 | 串口屏 |
| 最高画质需求 | HDMI |

---

## 二、硬件连接

### 典型接线（以淘晶驰 / Nextion 为例）

```
FPGA                         串口屏
┌────────┐                 ┌────────┐
│ UART TX ├───────────────→│ RX     │
│ UART RX │←───────────────│ TX     │（可选，接收按键数据）
│   GND   ├───────────────│ GND    │
│   VCC   ├───────────────│ VCC    │（5V / 3.3V，视屏幕规格）
└────────┘                 └────────┘
```

### ACX720 开发板 UART 引脚

| 信号 | FPGA 引脚 | IOSTANDARD |
|------|:---------:|:----------:|
| FPGA_UART_TX | M15 | LVCMOS33 |
| FPGA_UART_RX | J21 | LVCMOS33 |

> 串口屏通常需要 5V 供电，FPGA 提供不了大电流，建议外接 USB 或独立电源给屏幕供电。

### 波特率

| 分辨率 | 建议波特率 | 约算帧率 |
|:------:|:---------:|:--------:|
| 480×272 | 115200 | ~15 fps |
| 800×480 | 921600 | ~20 fps |
| 1024×600 | 921600 或更高 | ~15 fps |

> 全屏刷纯色最快，刷图片最慢（数据量大）。

---

## 三、指令协议（淘晶驰 TJC / 类 Nextion）

### 通用帧格式

```
0xFF 0xFF 0xFF 为帧尾（3 字节）
指令 + 参数 + 0xFF 0xFF 0xFF
```

### 常用指令

| 指令 | 格式 | 说明 |
|:----:|------|------|
| 页面切换 | `page 页面名称` + 帧尾 | 切换当前页面 |
| 文本设置 | `t0.txt="Hello"` + 帧尾 | 控件 id 为 t0 的文本框赋值 |
| 数值设置 | `n0.val=123` + 帧尾 | 控件 id 为 n0 的数字控件赋值 |
| 进度条 | `j0.val=50` + 帧尾 | 控件 id 为 j0 的进度条设置值 |
| 图片切换 | `pic 0, 10` + 帧尾 | 控件 id 0 显示图片 10 |
| 颜色设置 | `t0.pco=63488` + 帧尾 | 设置文本颜色（RGB565） |
| 可见性 | `t0.vis=1` + 帧尾 | 显示/隐藏控件 |

> 具体指令集以屏幕型号的指令手册为准，各品牌有差异。

### RGB565 颜色编码

```
高位         低位
  RRRR RGGG GGGG BBBB
   bit15-11  bit10-5  bit4-0

常用颜色：
  红 = 0xF800  (11111 000000 00000)
  绿 = 0x07E0  (00000 111111 00000)
  蓝 = 0x001F  (00000 000000 11111)
  白 = 0xFFFF
  黑 = 0x0000
```

---

## 四、FPGA 实现方案

### 架构

```
UART 发送器（复用笔记14 UART_TX）+ 指令拼接逻辑

   ┌──────────────┐     ┌──────────────┐
   │ 指令拼接 FSM │────→│ UART_Byte_TX │────→ TX 引脚
   │ 控制何时发   │     │ 波特率 115200│
   │ 哪条指令     │     └──────────────┘
   └──────────────┘
```

### 实现步骤

1. **复现 [14UART串口发送模块设计与验证] 的 `Uart_Byte_Tx` 模块**
   - 波特率 115200（从 50MHz 分频：`50_000_000 / 115200 ≈ 434`）
   - 每次发送 1 字节，发送完成后返回完成信号

2. **编写指令拼接模块**
   - 按需求生成字符串（如 `n0.val=123`）
   - 将字符串逐字节送入 UART 发送器
   - 帧尾追加 `0xFF 0xFF 0xFF`

3. **上电初始化流程**
   - 延时等待屏幕启动（~500ms）
   - 发页面切换指令
   - 定期刷新显示内容

### 核心代码

```verilog
module screen_driver (
    input  wire        clk_50m,
    input  wire        rst_n,
    output reg         uart_tx,
    input  wire        uart_rx   // 暂不使用（读触摸/按键时需要）
);

    parameter BAUD_CNT = 50_000_000 / 115200 - 1;  // 434

    // UART 字节发送器（完整版见笔记14）
    uart_byte_tx #(.BAUD_CNT(BAUD_CNT)) u_tx (
        .clk   (clk_50m),
        .rst_n (rst_n),
        .send  (send_en),
        .data  (send_data),
        .tx    (uart_tx),
        .done  (send_done)
    );

    // 指令发送 FSM
    localparam IDLE    = 0;
    localparam SEND_STR = 1;
    localparam SEND_FF  = 2;
    localparam DONE     = 3;

    reg [3:0] state, byte_idx;
    reg [7:0] cmd_mem[0:31];  // 指令缓冲区
    reg [4:0] cmd_len;

    // 示例：发送 "n0.val=123" + 0xFF 0xFF 0xFF
    always @(posedge clk_50m or negedge rst_n) begin
        if (!rst_n) begin
            state <= IDLE;
            uart_tx <= 1'b1;
        end else case (state)
            IDLE: begin
                // 初始化指令
                cmd_mem[0] <= "n"; cmd_mem[1] <= "0";
                cmd_mem[2] <= "."; cmd_mem[3] <= "v";
                cmd_mem[4] <= "a"; cmd_mem[5] <= "l";
                cmd_mem[6] <= "="; cmd_mem[7] <= "1";
                cmd_mem[8] <= "2"; cmd_mem[9] <= "3";
                cmd_len <= 10;
                byte_idx <= 0;
                state <= SEND_STR;
            end

            SEND_STR: begin
                if (byte_idx < cmd_len) begin
                    if (send_done) begin
                        byte_idx <= byte_idx + 1;
                    end else begin
                        send_en <= 1;
                        send_data <= cmd_mem[byte_idx];
                    end
                end else begin
                    byte_idx <= 0;
                    state <= SEND_FF;
                end
            end

            SEND_FF: begin
                if (byte_idx < 3) begin
                    if (send_done) begin
                        byte_idx <= byte_idx + 1;
                    end else begin
                        send_en <= 1;
                        send_data <= 8'hFF;
                    end
                end else begin
                    state <= DONE;
                end
            end

            DONE: begin
                // 发送完成，等待下次触发
            end
        endcase
    end

endmodule
```

### 发送字符串的简便方法

如果屏幕支持直接发 ASCII 字符串，可以将指令预先编码为字节数组，用 ROM 或者 case 语句存储多条指令：

```verilog
// 指令 ROM：索引 0~N，每条指令以 0xFF 0xFF 0xFF 结尾
reg [7:0] cmd_rom[0:63];

initial begin
    // page 0
    cmd_rom[0]  = "p"; cmd_rom[1]  = "a"; cmd_rom[2]  = "g"; cmd_rom[3]  = "e";
    cmd_rom[4]  = " "; cmd_rom[5]  = "0";
    cmd_rom[6]  = 8'hFF; cmd_rom[7]  = 8'hFF; cmd_rom[8]  = 8'hFF;
    // t0.txt="FPGA"
    cmd_rom[9]  = "t"; cmd_rom[10] = "0"; cmd_rom[11] = ".";
    cmd_rom[12] = "t"; cmd_rom[13] = "x"; cmd_rom[14] = "t";
    cmd_rom[15] = "="; cmd_rom[16] = "\"";
    cmd_rom[17] = "F"; cmd_rom[18] = "P"; cmd_rom[19] = "G"; cmd_rom[20] = "A";
    cmd_rom[21] = "\"";
    cmd_rom[22] = 8'hFF; cmd_rom[23] = 8'hFF; cmd_rom[24] = 8'hFF;
    // ...
end
```

---

## 五、FPGA 更新显示的策略

串口屏的帧率不是由 FPGA 决定的，FPGA 只负责发指令，屏幕内部自行绘制。因此要合理控制发送频率：

### 策略 1：定时刷新

```verilog
// 每 50ms 刷新一次数值
reg [21:0] timer;
always @(posedge clk_50m or negedge rst_n) begin
    if (!rst_n) timer <= 0;
    else timer <= timer + 1;
end

wire time_up = (timer == 22'd2_500_000);  // 50ms @50MHz
```

### 策略 2：事件触发

只在需要时才发送指令（如 ADC 值变化超过阈值），减少无意义的 UART 通信。

### 策略 3：全屏刷新的替代方案

- 尽量用**局部更新**代替全屏刷新
- 用屏幕的「页面切换」功能预置多种布局
- 用「控件可见性」切换显示/隐藏

---

## 六、关键注意事项

### 1. 波特率匹配

FPGA 的波特率发生器必须和屏幕设置一致。屏幕通常在上电时通过配置文件设置波特率。

```verilog
// 50MHz → 115200 的分频值
localparam BAUD_DIV = 50_000_000 / 115200 / 16;  // 若使用 16 倍过采样
// 或直接按位周期分频：
localparam BAUD_DIV = 50_000_000 / 115200 - 1;    // = 433
```

### 2. 电源问题

串口屏在刷屏时电流较大（~200mA），**不要从 FPGA 的 3.3V 引脚取电**。建议：
- 5V USB 供电
- 独立 5V 电源适配器
- 与 FPGA 共地即可

### 3. 逻辑电平

确认串口屏的 UART 电平：
- 5V 屏幕 → FPGA 3.3V 输出通常可直连（TTL 兼容），但 RX 端可能需要分压
- 3.3V 屏幕 → 直连

### 4. 流控处理

发送两条指令之间需要留有时间间隔（通常 5~20ms），让屏幕处理完当前命令：

```verilog
reg [19:0] gap_cnt;
always @(posedge clk_50m) begin
    if (send_done) gap_cnt <= 20'd500_000;  // 10ms 间隔
    else if (gap_cnt > 0) gap_cnt <= gap_cnt - 1;
end

wire gap_ok = (gap_cnt == 0);  // 允许发下一条
```

### 5. 指令编码

中文指令字符串在 Verilog 中要用 UTF-8 编码，确保 UART 发送的是屏幕能识别的字节序列（通常屏幕用 GBK 编码）。

---

## 七、与已有笔记的关联

| 笔记 | 关联点 |
|------|--------|
| [[14UART串口发送模块设计与验证]] | 串口屏的核心发送器，直接复用 `Uart_Byte_Tx` |
| [[15UART串口接收模块设计与验证]] | 接收触摸/按键数据时需要此模块 |
| [[16UART回环LoopBack设计与验证]] | 调试串口屏通信时可用于验证线路 |
| [[23VGA 显示驱动详解]] | 对比直驱与串口屏两种显示方案 |
| [[24HDMI 显示驱动详解]] | 对比直驱与串口屏两种显示方案 |
| [[20FPGA ROM 详解]] | 指令表可存入 ROM，用索引访问 |

---

## 参考

- 淘晶驰 TJC 指令手册
- Nextion Instruction Set
- 迪文 DWIN 协议文档
- ACX720 管脚分配表（UART 引脚 M15/J21）
