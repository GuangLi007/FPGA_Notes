# 数码管 74HC595 动态扫描显示

## 硬件原理

ACX720 开发板使用 **两片 74HC595 级联** 驱动 8 位数码管：

```
FPGA_DIO → [595#A (段码)] → Q7' → [595#B (位选)]
FPGA_SCLK → 两片共享 (移位时钟)
FPGA_RCLK → 两片共享 (锁存时钟)
```

| 信号 | FPGA 引脚 | 说明 |
|------|-----------|------|
| `ds` (DIO) | M18 | 串行数据 |
| `sh_cp` (SCLK) | F4 | 移位时钟 (上升沿移入) |
| `st_cp` (RCLK) | C2 | 锁存时钟 (上升沿输出) |

### 74HC595 时序

```
SCLK  ̅___̅___̅___̅___̅___̅___̅___̅___̅___̅___̅___̅___̅___̅___̅___̅___̅_
DIO   ----< bit15 >< bit14 >< bit13 > ... < bit1  >< bit0  >-----
RCLK  ___________________________________________________̅________
```

- MSB 先发，SCLK 上升沿逐位移入
- 16 位全部移完后，RCLK 上升沿锁存到输出

### 数据格式

```
data[15:0] = {1'b0, seg[6:0], sel[7:0]}
```

- `sel[7:0]` → 595#A (位选，低电平选中)
- `{1'b0, seg[6:0]}` → 595#B (段码，共阳极，低电平点亮)

### 段码表 (共阳极, 0=亮, seg={g,f,e,d,c,b,a})

| 显示 | 段码 | 显示 | 段码 |
|------|------|------|------|
| 0 | 1000000 | 8 | 0000000 |
| 1 | 1111001 | 9 | 0010000 |
| 2 | 0100100 | A | 0001000 |
| 3 | 0110000 | b | 0000011 |
| 4 | 0011001 | C | 1000110 |
| 5 | 0010010 | d | 0100001 |
| 6 | 0000010 | E | 0000110 |
| 7 | 1111000 | F | 0001110 |

## 动态扫描

人眼视觉暂留 ≈ 20ms。8 个数码管轮流点亮，每位数码管点亮 1ms：

```
位选: 10000000 → 01000000 → 00100000 → ... → 00000001 → (循环)
       digit7     digit6     digit5           digit0
       (1ms)      (1ms)      (1ms)           (1ms)
```

扫描周期 = 8ms，刷新率 = 125Hz（> 60Hz 无闪烁）。

## 工程结构

```
Segment_Display/
└── Segment_Display.srcs/sources_1/new/
    ├── Top.v              # 顶层模块
    ├── Seg_Control.v      # 分频 + 扫描 + 段码译码
    └── Hc595_Driver.v     # 74HC595 串行驱动
```

### Top.v

```verilog
module Top (
    input  wire       clk,       // 50MHz
    input  wire       reset_n,   // 复位
    input  wire [7:0] sw,        // 拨码开关
    output wire       ds,        // 595 数据
    output wire       sh_cp,     // 595 移位时钟
    output wire       st_cp      // 595 锁存时钟
);
```

### Seg_Control.v

- 50MHz → 1ms 定时器 (`scan_cnt == 49999`)
- 每 1ms 切换一位 (`sel` 循环左移)
- 显示内容映射：
  - 右起第 1 位：`sw[3:0]` (hex)
  - 右起第 2 位：`sw[7:4]` (hex)
  - 左起 6 位：固定 `123456`

### Hc595_Driver.v

计数器 0~33 驱动 595 时序：

- 奇数值：`sh_cp = 1` (上升沿移入)
- 偶数值 (0~30)：`sh_cp = 0`，设置下一位数据
- `cnt = 33`：`st_cp = 1` (锁存)

每帧传输耗时 34 周期 × 20ns = 680ns。

## 验证结果

| 操作 | 显示结果 |
|------|---------|
| SW = 0x00 | 数码管显示 `12345600` |
| SW = 0x01 | 数码管显示 `12345601` |
| SW = 0xAB | 数码管显示 `123456Ab` |
| SW = 0xFF | 数码管显示 `123456FF` |

左 6 位固定显示 `123456`，右 2 位随拨码开关动态变化。

## 相关文件

- 工程路径：`/home/kl/My_Project/FPGA/Segment_Display/`
- 参考例程：`/home/kl/资料/小梅哥FPGA/02 设计实例/XC7A35T/ch12_acx720_hex8_hc595.rar`
