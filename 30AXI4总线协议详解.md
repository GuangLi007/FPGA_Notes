---
tags:
  - FPGA
  - AXI4
  - AMBA
  - 总线协议
  - Xilinx
---

## 核心结论

- **AXI4** = Advanced eXtensible Interface，ARM AMBA 4.0 总线标准
- 三种类型：**AXI4**（内存映射，支持突发）、**AXI4-Lite**（内存映射，单次传输）、**AXI4-Stream**（数据流，无地址）
- 五个独立通道：**读地址**、**读数据**、**写地址**、**写数据**、**写响应**
- 核心握手：**VALID / READY** 机制，上下游均可控制传输速率
- Xilinx IP（MIG、DMA、VDMA、FIFO）全部采用 AXI4 接口，掌握它是使用 Xilinx FPGA 的必备技能
- 高级专题：[[31AXI4_Atomic原子操作与排序模型]]、[[32AXI4_LowPower低功耗与性能优化]]

---

## 一、AXI4 概览

### 协议族

| 类型 | 地址 | 突发 | 适用场景 |
|:----:|:----:|:----:|----------|
| **AXI4** (Full) | ✅ | ✅ 支持 | DDR 控制器、DMA、高带宽主存访问 |
| **AXI4-Lite** | ✅ | ❌ 单次传输 | 控制寄存器、状态寄存器、配置接口 |
| **AXI4-Stream** | ❌ | ✅ 连续流 | FIFO、视频流、数据管道 |

### 典型系统拓扑

```
┌────────┐   AXI4    ┌──────────┐   AXI4    ┌──────────┐
│ Cortex │←────────→│ AXI      │←────────→│  DDR3    │
│ A9/M3  │          │ Interconnect │          │  (MIG)   │
└────────┘          └─────┬────┘          └──────────┘
                          │
               ┌──────────┼──────────┐
               │                    │
          ┌────▼────┐         ┌────▼────┐
          │ AXI DMA │         │ AXI GPIO│
          │ (数据搬移)│         │ (控制)   │
          └─────────┘         └─────────┘
```

---

## 二、AXI4 通道与信号

### 5 个独立通道

```
写事务:
  ┌──────┐  AW通道  ┌──────┐
  │      │──地址──→│      │
  │ Master│  W通道   │ Slave│
  │      │──数据──→│      │
  │      │  B通道   │      │
  │      │←─响应──│      │
  └──────┘         └──────┘

读事务:
  ┌──────┐  AR通道  ┌──────┐
  │      │──地址──→│      │
  │ Master│  R通道   │ Slave│
  │      │←─数据──│      │
  └──────┘         └──────┘
```

### 全局信号

| 信号 | 说明 |
|:----:|------|
| ACLK | 总线时钟，所有信号同步于上升沿 |
| ARESETn | 异步复位，低有效 |
| ACLKEN | 时钟使能（可选） |

### 写地址通道 (AW)

|   信号    |  位宽   | 说明                                       |
| :-----: | :---: | ---------------------------------------- |
|  AWID   |  xID  | 写地址 ID（用于乱序）                             |
| AWADDR  | 32/64 | 写首地址                                     |
|  AWLEN  |   8   | 突发长度（0=1 拍，1=2 拍，... 255=256 拍）          |
| AWSIZE  |   3   | 每拍字节数：000=1, 001=2, 010=4, 011=8, 100=16 |
| AWBURST |   2   | 突发类型：00=FIXED, 01=INCR, 10=WRAP          |
| AWVALID |   1   | 主→从：地址有效                                 |
| AWREADY |   1   | 从→主：地址就绪                                 |

### 写数据通道 (W)

| 信号 | 位宽 | 说明 |
|:----:|:----:|------|
| WDATA | 32/64/128 | 写数据总线 |
| WSTRB | WDATA/8 | 写选通（每字节 1 bit，1=该字节写入） |
| WLAST | 1 | 最后一拍数据 |
| WVALID | 1 | 主→从：数据有效 |
| WREADY | 1 | 从→主：数据就绪 |

### 写响应通道 (B)

|   信号   | 位宽  | 说明                                          |
| :----: | :-: | ------------------------------------------- |
|  BID   | xID | 响应 ID                                       |
| BRESP  |  2  | 响应：00=OKAY, 01=EXOKAY, 10=SLVERR, 11=DECERR |
| BVALID |  1  | 从→主：响应有效                                    |
| BREADY |  1  | 主→从：响应就绪                                    |

### 读地址通道 (AR)

|   信号    |  位宽   | 说明       |
| :-----: | :---: | -------- |
|  ARID   |  xID  | 读地址 ID   |
| ARADDR  | 32/64 | 读首地址     |
|  ARLEN  |   8   | 突发长度     |
| ARSIZE  |   3   | 每拍字节数    |
| ARBURST |   2   | 突发类型     |
| ARVALID |   1   | 主→从：地址有效 |
| ARREADY |   1   | 从→主：地址就绪 |

### 读数据通道 (R)

| 信号 | 位宽 | 说明 |
|:----:|:----:|------|
| RID | xID | 数据 ID |
| RDATA | 32/64/128 | 读数据 |
| RRESP | 2 | 响应 |
| RLAST | 1 | 最后一拍 |
| RVALID | 1 | 从→主：数据有效 |
| RREADY | 1 | 主→从：数据就绪 |

---

## 三、握手协议 (VALID / READY)

### 握手规则

```
VALID 和 READY 的三种握手场景:

1. VALID 先有效，READY 后有效 (最常用)
   CLK  ┌┐┌┐┌┐┌┐┌┐┌┐
   VALID  ___/███_______
   READY  _______/███___
   DATA   XXX/  D  XXXXX
              ↑ 这里传输发生

2. READY 先有效，VALID 后有效
   CLK  ┌┐┌┐┌┐┌┐┌┐┌┐
   VALID  _______/███___
   READY  ___/███_______
   DATA   XXXXXXX/  D  X
                  ↑ 这里传输发生

3. VALID 和 READY 同时有效
   CLK  ┌┐┌┐┌┐┌┐┌┐┌┐
   VALID  ___/███_______
   READY  ___/███_______
   DATA   XXX/  D  XXXXX
              ↑ 这里传输发生
```

### 依赖关系

```
写事务通道顺序:
  1. Master 发 AWADDR (AWVALID=1)，等待 AWREADY
  2. Master 发 WDATA (WVALID=1)，等待 WREADY
  3. W 通道可与 AW 通道独立（数据可先于地址发出）
  4. Slave 完成写后发 BRESP (BVALID=1)，Master 回复 BREADY=1
  5. Master 必须在收到 BVALID 后才能发下一个同 ID 写事务

读事务通道顺序:
  1. Master 发 ARADDR (ARVALID=1)，等待 ARREADY
  2. Slave 返回 RDATA (RVALID=1)，Master 回复 RREADY=1
  3. 最后一拍 RLAST=1
```

### 关键规则

- VALID 一旦拉高，**不可撤销**（必须等到握手完成）
- READY 可以拉高后再次拉低（= 这次来不及，下次）
- Slave 可依赖 VALID 再回复 READY（= 看到有效数据再准备）
- 通道之间无固定时序依赖（W 数据可早于 AW 地址）

---

## 四、突发传输

### 突发参数

```
AWADDR = 0x1000
AWLEN  = 3        → 4 拍数据
AWSIZE = 010      → 4 字节/拍 (32bit)
AWBURST = 01      → INCR (递增)

地址计算:
  拍0: 0x1000
  拍1: 0x1004  (0x1000 + 4)
  拍2: 0x1008  (0x1004 + 4)
  拍3: 0x100C  (0x1008 + 4)
```

### 突发类型

| BURST | 类型 | 地址变化 |
|:-----:|:----:|----------|
| 00 | **FIXED** | 地址不变（适合 FIFO） |
| 01 | **INCR** | 地址递增（最常用，适合连续内存） |
| 10 | **WRAP** | 地址递增到边界后回绕（适合 Cache Line） |
| 11 | 保留 | - |

### INCR 地址计算

```
ADDR_n = ADDR_start + (2^SIZE) × n    (n = 0, 1, ..., LEN)

例: ADDR=0x1000, SIZE=011(8B), LEN=7(8拍)
  0x1000, 0x1008, 0x1010, 0x1018, 0x1020, 0x1028, 0x1030, 0x1038
```

### WRAP 地址计算

```
WRAP 地址在 [Lower, Upper] 区间内循环:
  Lower = INT(ADDR / (2^SIZE × LEN)) × (2^SIZE × LEN)
  Upper = Lower + (2^SIZE × LEN) - 1
  Size  = 2^SIZE

例: ADDR=0x100C, SIZE=2(4B), LEN=3(4拍), INCR:
  0x100C, 0x1010, 0x1014, 0x1018

例: ADDR=0x100C, SIZE=2(4B), LEN=3(4拍), WRAP:
  Lower = INT(0x100C / (4×4)) × 16 = 0x1000
  Upper = 0x100F
  0x100C, 0x1000, 0x1004, 0x1008  ← 到 0x100F 后回绕到 0x1000
```

### 边界 (AxSIZE × AxLEN 不可跨 4K 边界)

AXI4 规定突发不能跨越 **4KB 地址边界**。4KB = 页大小，跨页事务必须拆分为两个独立突发。

---

## 五、Cache 缓存策略 (ARCACHE / AWCACHE)

AXI4 通过地址通道的 **ARCACHE**（读）和 **AWCACHE**（写）信号控制缓存策略与存储器属性，是 CPU 与总线之间 Cache Coherency 的关键。

### AxCACHE[3:0] 信号含义

|  位  |         名称         | 说明                                                                |
| :-: | :----------------: | ----------------------------------------------------------------- |
| [0] |   **Bufferable**   | 0=Non-bufferable（写直达，必须完成后才能发下一个）；1=Bufferable（写可缓冲，Slave 稍后完成即可） |
| [1] |   **Cacheable**    | 0=Non-cacheable（不可缓存）；1=Cacheable（可缓存，允许读取合并/预取）                  |
| [2] | **Read Allocate**  | 0=读不分配缓存行；1=读缺失时分配缓存行                                             |
| [3] | **Write Allocate** | 0=写不分配缓存行；1=写缺失时分配缓存行                                             |

### 常见 AxCACHE 组合

| 值 | 存储器类型 | Bufferable | Cacheable | 说明 |
|:--:|:----------:|:----------:|:---------:|------|
| 0000 | **Device Non-bufferable** | ❌ | ❌ | 严格有序，每次访问直达外设，适合控制寄存器 |
| 0001 | **Device Bufferable** | ✅ | ❌ | 写可合并缓冲，读仍直达，适合帧缓存等 |
| 0010 | **Normal Non-cacheable** | ✅ | ❌ | 普通内存但不可缓存，写可合并 |
| 0011 | **Normal Write-Through** | ✅ | ✅ | 写直达 + 读缓存，分配策略由 RA/WA 决定 |
| 0111 | **Normal Write-Back** | ✅ | ✅ | 写回 + 读写分配，最高性能，适合 DDR |
| 1111 | **Normal Write-Back** | ✅ | ✅ | 同上，全分配策略 |

### AxDOMAIN[1:0] 共享域

DOMAIN 信号指定事务的共享范围，影响 Cache Coherency：

| 值 | 域 | 说明 |
|:--:|:----|------|
| 00 | **Non-shareable** | 只有当前处理器核访问，无需一致性 |
| 01 | 保留 | — |
| 10 | **Inner Shareable** | 同簇内多核共享一致性域 |
| 11 | **Outer Shareable** | 系统级多簇共享一致性域 |

### 典型配置

```
DDR 主存 (高性能):
  AWCACHE = 4'b0111  (Write-Back, Read+Write Allocate)
  ARCACHE = 4'b0111  (Write-Back, Read+Write Allocate)
  AxDOMAIN = 2'b00   (Non-shareable, 单核系统)

MMIO 控制寄存器:
  AWCACHE = 4'b0000  (Device Non-bufferable)
  ARCACHE = 4'b0000  (Device Non-bufferable)

视频帧缓冲 (写合并):
  AWCACHE = 4'b0001  (Device Bufferable)
  ARCACHE = 4'b0000  (Device Non-bufferable)
```

### Cache 策略选择建议

```
使用 Write-Back:
  - DDR / DRAM 主存
  - 性能敏感的批量数据

使用 Write-Through:
  - 共享内存（多核写冲突场景）
  - 需要实时可见性的通信缓冲区

使用 Device Non-bufferable:
  - 控制/状态寄存器
  - 对顺序敏感的外设 MMIO

使用 Device Bufferable:
  - 帧缓存（只写不读）
  - FIFO 写入端
```

---

## 六、Protection 保护机制 (AxPROT)

AXI4 通过地址通道的 **AxPROT[2:0]** 信号提供访问权限控制，用于系统级安全隔离。

### AxPROT[2:0] 信号

| 位 | 名称 | 说明 |
|:--:|:----:|------|
| [0] | **Privileged** | 0=User 模式；1=Privileged 模式（Supervisor/OS） |
| [1] | **Secure** | 0=Secure 空间；1=Non-Secure 空间（TrustZone） |
| [2] | **Instruction** | 0=Data 访问；1=Instruction 取指 |

### 编码组合

| AxPROT | 模式 | 安全 | 类型 | 典型场景 |
|:------:|:----:|:----:|:----:|----------|
| 000 | User | Secure | Data | 用户态数据访问（安全世界） |
| 001 | User | Secure | Inst | 用户态取指（安全世界） |
| 010 | User | Non-Secure | Data | 用户态数据访问（非安全世界） |
| 011 | User | Non-Secure | Inst | 用户态取指（非安全世界） |
| 100 | Privileged | Secure | Data | 内核态数据访问（安全世界） |
| 101 | Privileged | Secure | Inst | 内核态取指（安全世界） |
| 110 | Privileged | Non-Secure | Data | 内核态数据访问（非安全世界） |
| 111 | Privileged | Non-Secure | Inst | 内核态取指（非安全世界） |

### TrustZone 隔离

ARM TrustZone 利用 AxPROT[1] 实现物理隔离：

```
Secure World                    Non-Secure World
┌────────────────────┐         ┌────────────────────┐
│ Secure OS          │         │ Rich OS (Linux)    │
│ Secure App         │         │ Normal App         │
│ Secure Memory      │         │ Non-Secure Memory  │
│ Secure Peripherals │         │ Non-Secure Periph  │
└────────────────────┘         └────────────────────┘
         │                              │
Secure AXI Master           Non-Secure AXI Master
         │                              │
         └──────────┬───────────────────┘
                    │
            ┌───────▼────────┐
            │   AXI          │
            │  Interconnect  │
            │ (TrustZone 过滤)│
            └───────┬────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
   Secure Slave          Non-Secure Slave
   (TZ 感知)              (TZ 不感知)
```

### FPGA 设计中的 PROT

在 Xilinx FPGA 设计中：
- PS（ZYNQ Processing System）发出的 AXI 事务携带 AxPROT，反映当前 CPU 运行模式
- PL（Programmable Logic）自定义 Master 通常固定 AxPROT=3'b000 或 3'b010
- AXI Interconnect 可根据 AxPROT 做 TrustZone 地址空间过滤
- 无安全需求的简单设计可忽略 AxPROT，Slave 端直接忽略该信号

```
忽略 PROT 的从机实现:
  assign awprot_ignored = 3'b0;  // 不采样 AWPROT
  assign arprot_ignored = 3'b0;  // 不采样 ARPROT
  从机所有地址空间对所有 Master 开放。
```

---

## 七、AXI4-Lite

AXI4-Lite 是 AXI4 的子集，用于控制寄存器读写：

### 与 AXI4 的差异

| 特性 | AXI4 | AXI4-Lite |
|:----:|:----:|:---------:|
| 突发 | ✅ | ❌（固定单拍） |
| AxLEN | 1~256 | 必须为 0（1 拍） |
| AxSIZE | 任意 | 必须匹配数据位宽 |
| AxID | 支持 | 可选（通常固定） |
| WSTRB | 支持 | 支持 |
| BRESP | OKAY/SLVERR/DECERR | 同上 |

### 典型用途

```
AXI4-Lite Master                  AXI4-Lite Slave
┌────────────┐                   ┌────────────┐
│  CPU       │  AWADDR  ──────→ │  寄存器 0   │
│  (Cortex)  │  WDATA   ──────→ │  寄存器 1   │
│            │  BRESP   ←────── │  寄存器 2   │
│            │  ARADDR  ──────→ │  寄存器 3   │
│            │  RDATA   ←────── │             │
└────────────┘                   └────────────┘
```

### Verilog 从机框架

```verilog
module axi_lite_slave #(
    parameter C_S_AXI_ADDR_WIDTH = 32,
    parameter C_S_AXI_DATA_WIDTH = 32
) (
    input  wire                        S_AXI_ACLK,
    input  wire                        S_AXI_ARESETN,
    // 写地址
    input  wire [C_S_AXI_ADDR_WIDTH-1:0] S_AXI_AWADDR,
    input  wire                        S_AXI_AWVALID,
    output reg                         S_AXI_AWREADY,
    // 写数据
    input  wire [C_S_AXI_DATA_WIDTH-1:0] S_AXI_WDATA,
    input  wire [C_S_AXI_DATA_WIDTH/8-1:0] S_AXI_WSTRB,
    input  wire                        S_AXI_WVALID,
    output reg                         S_AXI_WREADY,
    // 写响应
    output reg  [1:0]                  S_AXI_BRESP,
    output reg                         S_AXI_BVALID,
    input  wire                        S_AXI_BREADY,
    // 读地址
    input  wire [C_S_AXI_ADDR_WIDTH-1:0] S_AXI_ARADDR,
    input  wire                        S_AXI_ARVALID,
    output reg                         S_AXI_ARREADY,
    // 读数据
    output reg  [C_S_AXI_DATA_WIDTH-1:0] S_AXI_RDATA,
    output reg  [1:0]                  S_AXI_RRESP,
    output reg                         S_AXI_RVALID,
    input  wire                        S_AXI_RREADY
);
    // 状态机
    localparam IDLE = 0, WR_ADDR = 1, WR_DATA = 2, WR_RESP = 3;

    reg [3:0] state;

    always @(posedge S_AXI_ACLK or negedge S_AXI_ARESETN) begin
        if (!S_AXI_ARESETN) begin
            state <= IDLE;
            S_AXI_AWREADY <= 1'b0;
            S_AXI_WREADY  <= 1'b0;
            S_AXI_BVALID  <= 1'b0;
            S_AXI_ARREADY <= 1'b0;
            S_AXI_RVALID  <= 1'b0;
        end else case (state)
            IDLE: begin
                if (S_AXI_AWVALID) begin
                    S_AXI_AWREADY <= 1'b1;
                    state <= WR_ADDR;
                end else if (S_AXI_ARVALID) begin
                    S_AXI_ARREADY <= 1'b1;
                    S_AXI_RDATA  <= reg_array[S_AXI_ARADDR[3:0]];
                    S_AXI_RRESP  <= 2'b00;
                    S_AXI_RVALID <= 1'b1;
                    state <= IDLE;  // 单周期读
                end
            end

            WR_ADDR: begin
                S_AXI_AWREADY <= 1'b0;
                if (S_AXI_WVALID) begin
                    S_AXI_WREADY <= 1'b1;
                    // 写寄存器 (WSTRB 控制字节使能)
                    for (int i = 0; i < C_S_AXI_DATA_WIDTH/8; i++) begin
                        if (S_AXI_WSTRB[i])
                            reg_array[S_AXI_AWADDR[3:0]][i*8 +: 8] <= S_AXI_WDATA[i*8 +: 8];
                    end
                    state <= WR_RESP;
                end
            end

            WR_RESP: begin
                S_AXI_WREADY <= 1'b0;
                S_AXI_BRESP  <= 2'b00;
                S_AXI_BVALID <= 1'b1;
                if (S_AXI_BREADY) begin
                    S_AXI_BVALID <= 1'b0;
                    state <= IDLE;
                end
            end
        endcase
    end
endmodule
```

---

## 八、AXI4-Stream

AXI4-Stream 用于数据流传输，**无地址**、**无写响应**，只有一对 VALID/READY。

### 信号

| 信号 | 位宽 | 说明 |
|:----:|:----:|------|
| TVALID | 1 | 主→从：数据有效 |
| TREADY | 1 | 从→主：数据就绪 |
| TDATA | N | 数据（字对齐） |
| TKEEP | N/8 | 字节有效位（类似 WSTRB） |
| TLAST | 1 | 帧结束（包尾） |
| TSTRB | N/8 | 字节类型（保留/非保留） |
| TID | 可选 | 数据流 ID |
| TDEST | 可选 | 目的标识（路由用） |
| TUSER | 可选 | 用户定义侧带信号 |

### 核心规则

```
传输发生在 TVALID && TREADY 同时为 1 的时钟沿。

Master 传输流程:
  1. 置 TDATA，置 TVALID=1
  2. 等待 TREADY=1
  3. 时钟上升沿：数据传输
  4. 最后一拍置 TLAST=1

Slave 接收流程:
  1. 置 TREADY=1（可随时置回 0 暂停）
  2. 检测 TVALID && TREADY → 接收数据
  3. 检测 TLAST → 帧结束
```

### 时序

```
ACLK   ┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐
           │  │  │  │  │  │  │  │  │  │
TVALID   ___/███___________________
TREADY   ___________/███___________
TDATA    XXX/D0/XXXXXXX/D1/XXXXXXXX
                       ↑ 此处传输 D0
                       TREADY=TREADY 上升沿采样
```

### 典型应用：视频流

```
AXI4-Stream 视频线格式:

      Blanking           Active Line
┌──────────────────┐┌────────────────────┐
│                  ││  pixel0 | pixel1 | ...│
│    TUSER=1       ││  TUSER=0             │
│    (SOF)         ││                      │
│    TDATA=无效     ││  TDATA = {R,G,B}     │
│    TLAST=0       ││  TLAST=0 (非最后)    │
└──────────────────┘└────────────────────┘
                     TLAST=1 (行尾/帧尾)
```

Xilinx VDMA 通过 AXI4-Stream 传输视频帧。

---

## 九、乱序传输与排序模型 (Ordering Model)

AXI4 通过 **ID 机制**实现高效乱序传输。乱序能力是高带宽的关键，但设计人员必须理解其约束。

### ID 机制

每个事务携带 **AxID**（读 ARID / 写 AWID），响应返回对应 **xID**（读 RID / 写 BID）：

```
Master 连续发出两个事务:
  事务0: ARID=0, ADDR=0x1000  ← 先发
  事务1: ARID=1, ADDR=0x2000  ← 后发

Slave 返回顺序（可能乱序）:
  先回: RID=1, DATA=0x2000内容  ← 后发的事务先完成
  后回: RID=0, DATA=0x1000内容  ← 先发的事务后完成

Master 通过 RID 匹配响应 → 正确！
```

### 核心排序规则

| 规则 | 说明 |
|:----|------|
| **同 ID 保序** | 同一 ID 的事务必须按发出顺序完成，不可穿插 |
| **异 ID 乱序** | 不同 ID 的事务可以乱序完成和交错传输 |
| **读写的独立性** | 读通道和写通道独立操作，互不影响 |
| **数据依赖** | 同地址的写后读必须保序（先写再读） |

### 事务间排序 (Transaction Ordering)

AXI4 协议定义了四种事务间关系：

```
Ordering 矩阵 (R=读, W=写):

       │  后 R,同ID │ 后 W,同ID │ 后 R,异ID │ 后 W,异ID
───────┼────────────┼────────────┼────────────┼────────────
前 R   │   保序      │   无要求    │   无要求    │   无要求
前 W   │   保序†     │   保序      │   无要求    │   无要求

† 同地址写后读必须保序（读要看到最新写入值）
```

### 写交错 (Write Interleaving)

```
写入交错允许不同 ID 的 WDATA 在数据通道上交错传输:

Master 0: AWID=0, 突发长度=4
Master 1: AWID=1, 突发长度=4

可能的数据通道时序:
  WDATA:  M0.D0 │ M1.D0 │ M0.D1 │ M1.D1 │ M0.D2 │ M1.D2 │ M0.D3 │ M1.D3
  WLAST:    0    │   0    │   0    │   0    │   0    │   0    │   1    │   1
  WID:      0    │   1    │   0    │   1    │   0    │   1    │   0    │   1

Slave 接收后按 WID 重组 → 正确！
```

写交错深度由 **Slave** 控制：Slave 通过 WREADY 控制接收节奏，无需支持所有交错深度。

### ID 分配策略

```
简单系统（单 Master，无需乱序）:
  ARID = 0, AWID = 0  → 所有事务保序

高性能 Master（DMA/GPU）:
  按通道/请求流分配不同 ID
  ARID = {req_id, channel_id}
  AWID = {req_id, channel_id}
  不同通道间可乱序，增加总线利用率

Xilinx IP 行为:
  - AXI DMA: 每个 channel 固定 ID，可配置
  - MIG: 内部使用多 ID 实现读写的乱序访问
  - AXI Interconnect: 透传 ID，或添加前缀位
```

### 乱序深度与性能

```
乱序深度（未完成的最大事务数）↔ 总线利用率:

乱序深度=1 (完全保序):
  ████████░░░░░░░░░░░░  ← 利用率约 40%
  每次等待前一个事务完成

乱序深度=8:
  ████████████████████  ← 利用率约 95%
  8 个事务并行，Slave 可自由调度

典型配置:
  - ZYNQ PS: 默认乱序深度较大（10+）
  - AXI DMA: 可配置 outstanding 数量
  - 自定义 Master: 建议至少支持 4 个 outstanding
```

### 关于原子操作与高级排序

AXI4 还定义了 **Locked Access**（锁访问）和 **Exclusive Access**（独占访问）两种原子操作机制，以及更复杂的排序模型。详见 [[31AXI4_Atomic原子操作与排序模型]]。

---

## 十、Xilinx AXI4 常见 IP

### 互联 / 桥接

| IP | 功能 |
|:---|------|
| **AXI Interconnect** | 多主多从交叉开关/总线矩阵 |
| **AXI SmartConnect** | 新一代 AXI 互联（替代 Interconnect） |
| **AXI Protocol Converter** | AXI3 ↔ AXI4 转换 |
| **AXI Data Width Converter** | 数据位宽转换（32↔64↔128） |
| **AXI Clock Converter** | 跨时钟域转换 |
| **AXI Register Slice** | 流水线寄存器（打破长路径时序） |

### 存储器/外设

| IP | 接口 | 功能 |
|:---|:----:|------|
| **MIG 7 Series** | AXI4 | DDR3 控制器（[[29DDR3内存详解]]） |
| **AXI BRAM Controller** | AXI4-Lite/Full | 将 Block RAM 映射到 AXI 空间 |
| **AXI UART Lite** | AXI4-Lite | 串口控制 |
| **AXI GPIO** | AXI4-Lite | 通用 IO |
| **AXI IIC** | AXI4-Lite | I²C Master（[[27I2C通信协议详解]]） |

### 数据搬移

| IP | 接口 | 功能 |
|:---|:----:|------|
| **AXI DMA** | AXI4 + AXI4S | 内存↔内存/外设数据搬移 |
| **AXI VDMA** | AXI4 + AXI4S | 视频帧缓冲区管理 |
| **AXI CDMA** | AXI4 | 内存到内存搬移（无外设接口） |

### 视频

| IP | 说明 |
|:---|------|
| **AXI4-Stream to Video Out** | Stream → 并行视频（RGB/DE/HS/VS） |
| **Video In to AXI4-Stream** | 视频输入 → Stream |
| **VDMA** | 视频帧缓冲控制器（DDR3 ↔ Stream） |

---

## 十一、FPGA 设计中的 AXI4

### 什么时候需要 AXI4

```
需要用 AXI4:
  - ZYNQ PS ↔ PL 通信
  - MIG DDR3 控制器接口
  - 视频帧缓冲（VDMA）
  - DMA 数据搬移
  - Xilinx IP 互连

不需要用 AXI4:
  - 纯逻辑模块（计数器、状态机、TMDS 编码）
  - 模块间简单数据传递（移位寄存器/同步 FIFO）
  - 无 CPU 的独立 FPGA 设计
```

### AXI4 接口封装建议

```verilog
// 将 AXI4 接口封装为简单的用户逻辑接口
// 用户模块不直接处理 AXI4 握手，通过 wrapper 转换

module my_logic_to_axi_wrapper (
    // AXI4 总线
    ...
    // 简化用户接口
    input  wire        user_wr_en,
    input  wire [31:0] user_wr_addr,
    input  wire [31:0] user_wr_data,
    output reg         user_wr_done,
    input  wire        user_rd_en,
    input  wire [31:0] user_rd_addr,
    output reg  [31:0] user_rd_data,
    output reg         user_rd_valid
);
    // 内部 AXI4 状态机
    // 将用户请求转为 AXI4 事务
endmodule
```

### 时序收敛注意事项

```
AXI4 长路径常见问题:
  1. Interconnect 内部逻辑多 → 路径过长
  2. AXI4 信号扇出大 → 时序恶化

解决方法:
  1. 插入 AXI Register Slice（打拍，增加延迟换时序）
  2. 降低 AXI4 时钟频率
  3. 减少 Interconnect 的 Master/Slave 端口数
  4. 启用 SmartConnect（比 Interconnect 时序更好）
```

---

## 十二、调试

### ILA 抓取 AXI 信号

```
Vivado 中添加 AXI Monitor:
  1. IP Catalog → AXI Verification IP
  2. 或手动添加 ILA + 自定义 AXI 触发条件

关键观察点:
  - VALID/READY 是否握手
  - WLAST/RLAST 是否在正确位置
  - BRESP/RRESP 是否为 OKAY
  - ARLEN/AWLEN 是否与期望一致
```

### 常见问题

| 现象 | 原因 | 解决 |
|:----|------|------|
| AWVALID 一直高但 AWREADY 不回应 | Slave 未就绪 | 检查 Slave 复位状态/配置寄存器 |
| WDATA 发出但 WREADY 始终为 0 | 写数据 FIFO 满 | 降低写入速率/增大 Slave 缓冲 |
| BVALID 一直为 0 | 写事务未完成 | 检查 WLAST 是否置位 |
| RDATA 与期望不符 | 地址错误/跨 4K 边界 | 检查地址映射和突发长度 |
| SLVERR 返回 | Slave 不支持该地址 | 检查地址范围 |
| DECERR 返回 | 地址无对应 Slave | 检查 Interconnect 地址分配 |
| 时序不收敛 | AXI 路径过长 | 插入 Register Slice |

---

## 参考

- ARM AMBA AXI4 Protocol Specification (IHI0022E)
- ARM AMBA AXI4-Stream Protocol Specification (IHI0051A)
- Xilinx UG1037: AXI Reference Guide
- Xilinx PG059: AXI Interconnect
- Xilinx PG201: AXI SmartConnect
- [[29DDR3内存详解]] — MIG 的 AXI4 用户接口
- [[21FPGA FIFO 详解]] — AXI4-Stream FIFO
- [[24HDMI 显示驱动详解]] — AXI4-Stream 视频流

## 高级主题

本章笔记为基础篇，涵盖协议核心机制。以下高级专题单独成篇：

| 笔记 | 内容 |
|:----|------|
| [[31AXI4_Atomic原子操作与排序模型]] | Locked/Exclusive 访问、事务间排序规则、ID 分配策略 |
| [[32AXI4_LowPower低功耗与性能优化]] | 低功耗接口 (CSYSREQ/CACTIVE)、带宽计算、优化技巧 |
