---
tags:
  - FPGA
  - DDR
  - 内存
  - MIG
  - Xilinx
---

## 核心结论

- **DDR SDRAM** = Double Data Rate Synchronous Dynamic Random Access Memory
- DDR 的本质：**时钟双沿采样**，每个 CLK 周期传输 2 次数据（比 SDR 快一倍）
- Xilinx 7 系列通过 **MIG (Memory Interface Generator)** IP 核驱动 DDR，XC7A35T 只有 **HR bank**（无 HP bank），DDR3 最大速率 1066 Mbps
- FPGA 中的 DDR 控制器 = 物理层 (PHY) + 控制器 + 用户接口；**用户接口推荐用 AXI4 或 Native 接口**

---

## 一、DDR 发展简史

|   代   |  推出  | VDDQ | 预读取 | 速率 (Mbps) | 关键变化                      |
| :---: | :--: | :--: | :-: | :-------: | ------------------------- |
| SDRAM | 1993 | 3.3V | 1n  |  66~133   | 单沿采样，并行                   |
|  DDR  | 2000 | 2.5V | 2n  |  200~400  | 双沿采样，DQS                  |
| DDR2  | 2003 | 1.8V | 4n  |  400~800  | OCD, ODT, 4n 预读取          |
| DDR3  | 2007 | 1.5V | 8n  | 800~2133  | CWL, ASR, ZQ 校准           |
| DDR4  | 2014 | 1.2V | 8n  | 1600~3200 | Bank Group, DBI, CRC      |
| DDR5  | 2020 | 1.1V | 16n | 3200~6400 | 每 DIMM 双通道, SPD Hub, PMIC |

---

## 二、DDR 核心原理

### SDRAM 内部结构

```
                ┌──────────────────────────────────────┐
                │           SDRAM 内部                  │
                │  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  │
Row Decoder ───→│ Bank0 │  │ Bank1 │  │ Bank2 │  │ Bank3 │  ← 多个 Bank
                │  └──┬──┘  └──┬──┘  └──┬──┘  └──┬──┘  │
                │     │        │        │        │      │
                └─────┴────────┴────────┴────────┴──────┘
                      │         │         │         │
                      └─────────┴─────────┴─────────┘
                               │
                         Column MUX & Sense Amp
                               │
                          Data Buffer
                               │
                            DQ pins
```

### 基本操作流程

```
ACTIVATE (行激活)
  ─→ 发送 Row Address + Bank → 该行数据被读到 Sense Amp (行缓冲)
      │
      ├── READ: 发送 Column Address → DQ 输出数据 (Burst)
      │
      ├── WRITE: 发送 Column Address + 写入数据 → DQ 输入
      │
      └── PRECHARGE: 关闭当前行，准备下一次 ACTIVATE
```

### DDR 的关键时序参数

| 参数 | 含义 | DDR3-1600 典型值 |
|:----:|------|:----------------:|
| tCK | 时钟周期 | 1.25 ns |
| tRCD | 行激活到读/写命令 | 13.75 ns (11 tCK) |
| tCL (CAS) | 读命令到数据输出 | 13.75 ns (11 tCK) |
| tRP | 预充电时间 | 13.75 ns (11 tCK) |
| tRAS | 行激活后最小行激活时间 | 35 ns (28 tCK) |
| tRC | 行循环时间 (tRAS + tRP) | 48.75 ns (39 tCK) |
| tRFC | 刷新时间 | ~300 ns |
| tCWL | CAS Write Latency（DDR3 特有） | 9 tCK |
| tWTR | 写入到读取延迟 | 7.5 ns (6 tCK) |

### 时序图（简化读操作）

```
CLK      ┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐
            │ │ │ │ │ │ │ │ │ │ │ │ │ │ │ │
CMD   ﹉﹉﹉┤ACT├﹉﹉﹉﹉﹉﹉├RD├﹉﹉﹉﹉﹉﹉﹉﹉|﹉﹉
            └┬┘           └┬┘
              │             │
ADDR  ﹉﹉﹉﹉﹉┤ROW├﹉﹉﹉﹉│COL├﹉﹉﹉﹉|﹉﹉
              └──┘         └──┘
              │             │◄── tCL ──►│
DQ    ﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉┤Q0├┤Q1├┤Q2├
                                      └──┘└──┘└──┘
DQS   ﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉┤\\├┤\\├┤\\├
                                      └──┘└──┘└──┘
              ◄── tRCD ──►
              ◄───────── tRAS ──────────►
```

### DDR 与 SDRAM 的核心区别

| 特性 | SDRAM | DDR3 |
|:----:|:-----:|:----:|
| 数据采样 | 时钟上升沿 | 双沿（上升+下降） |
| 数据选通 | 无 | DQS（差分/单端） |
| 预读取 | 1n | 8n（一次列地址读 8 个数据） |
| 输出驱动 | 无校准 | ZQ 校准 + ODT |
| 端接 | 板级 | 片内 ODT（On-Die Termination） |
| 命令编码 | /CS /RAS /CAS /WE | 改用 /CS + /RAS + /CAS + /WE |
| 电源 | 3.3V | 1.5V DDR3 / 1.2V DDR4 |

---

## 三、DDR3 详解

### 引脚定义

| 类型  |      信号       |  方向   | 功能                   |
| :-: | :-----------: | :---: | -------------------- |
| 时钟  |  CK_P / CK_N  | → DDR | 差分时钟（FPGA 输出）        |
| 数据  |    DQ[7:0]    |   ↔   | 双向数据线，每个字节组 8bit     |
| 选通  | DQS_P / DQS_N |   ↔   | 数据选通（FPGA 写时输出，读时输入） |
| 掩码  |      DM       | → DDR | 写掩码（DDR4 改为 DBI）     |
| 行地址 |    BA[2:0]    | → DDR | Bank 地址              |
| 行地址 |    A[15:0]    | → DDR | 行地址 / 列地址复用          |
| 控制  |      /CS      | → DDR | 片选                   |
| 控制  | /RAS /CAS /WE | → DDR | 命令编码                 |
| 控制  |      CKE      | → DDR | 时钟使能                 |
| 控制  |      ODT      | → DDR | 片内端接使能               |
| 控制  |    /RESET     | → DDR | 复位（DDR3 新增）          |
| 校准  |      ZQ       | → DDR | ZQ 校准参考电阻（240Ω 接地）   |

### 命令集

| 命令 | /CS | /RAS | /CAS | /WE | 说明 |
|:----:|:---:|:----:|:----:|:---:|------|
| DESELECT | H | X | X | X | 无操作 |
| NOP | L | H | H | H | 无操作 |
| ACTIVE | L | L | H | H | 行激活 |
| READ | L | H | L | H | 列读，带 auto-precharge |
| WRITE | L | H | L | L | 列写，带 auto-precharge |
| PRECHARGE | L | L | H | L | 关闭当前行 |
| REFRESH | L | L | L | H | 自动刷新 |
| MRS | L | L | L | L | 模式寄存器设置 |
| ZQCL | L | H | H | L | ZQ 长校准 |
| ZQCS | L | H | L | H | ZQ 短校准 |

### Burst 顺序

DDR3 固定 Burst Length = 8（BL8）。8 个数据顺序由以下决定：
- **Burst Order**: Sequential / Interleaved（MRS 配置）
- **读/写命令中使用 A12 或 A10 决定 auto-precharge**

```
Sequential (A3=A2=0, Burst=8)
  Column Start = 0: D0 D1 D2 D3 D4 D5 D6 D7
  Column Start = 4: D4 D5 D6 D7 D0 D1 D2 D3

Interleaved
  Column Start = 0: D0 D1 D2 D3 D4 D5 D6 D7
  Column Start = 4: D4 D5 D6 D7 D0 D1 D2 D3
```

DDR3 还支持 BL8 on the fly（通过 A12 在命令中切换 8 或 4）。

### 初始化序列

```
1. 上电 → 等待 CKE 稳定 (至少 500μs)
2. CKE = H
3. 等待至少 tXPR 后发 NOP
4. MRS (MR2): CWL, 写恢复等
5. MRS (MR3): 温度相关
6. MRS (MR1): ODT, 驱动强度, 附加延迟
7. MRS (MR0): CAS 延迟, 突发类型/长度
8. ZQCL: ZQ 长校准 (tZQinit ≈ 512 tCK)
9. 等待初始化完成
```

---

## 四、DDR4 详解

### DDR4 相对 DDR3 的变化

|     特性     |     DDR3     |             DDR4              |
| :--------: | :----------: | :---------------------------: |
|     供电     |     1.5V     |             1.2V              |
|    预读取     |      8n      | 8n（数据仍为 8n，但 Bank Group 提高带宽） |
| Bank Group |      无       |    4 个 BG，每个 BG 内 4 个 Bank    |
|    VREF    |   VREF 全局    |      VREFDQ 内建，每个字节组可独立       |
|    DBI     |      无       |   Data Bus Inversion（降低功耗）    |
|    CRC     |    无写 CRC    |           写 CRC 校验            |
|   CA 奇偶    |      无       |           命令/地址奇偶校验           |
|    ODT     |    ODT 静态    |      ODT 动态 + Vref 独立配置       |
|    最大容量    |    8Gb 芯片    |            16Gb 芯片            |
|     引脚     | 240 pin DIMM |         288 pin DIMM          |

### Bank Group 原理

DDR4 引入 Bank Group 后，不同 Bank Group 的访问可以**流水**：

```
Bank Group 0 ─→ Bank0 ... Rank0  ──→ DQ[7:0]
Bank Group 1 ─→ Bank0 ... Rank3  ──→ DQ[7:0]  ← 可与 BG0 并行
Bank Group 2 ─→ ...              ──→ DQ[7:0]
Bank Group 3 ─→ ...              ──→ DQ[7:0]

// 不同 BG 的连续读操作延迟 tCCD_L = 4 tCK
// 同 BG 内的连续读操作延迟 tCCD_S = 8 tCK (DDR4-2400+ 为 4-6)
```

### DDR4 的 DBI

DBI (Data Bus Inversion)：
- 如果 8bit DQ 中 0 的个数 > 4，则翻转数据并置位 DBI_n
- 目的：减少功耗（每个 bit 翻转 → 功耗变化）

```
写数据: 0xFF = 1111_1111 → 0 的个数 = 0 ≤ 4 → 不翻转
写数据: 0x00 = 0000_0000 → 0 的个数 = 8 > 4 → 翻转 → DBI=1, DQ=1111_1111
读数据: 接收方检测 DBI=1 → 翻转回来 → 0x00
```

---

## 五、DDR5 简介

DDR5 相对 DDR4 的重大变化：

|   特性    |   DDR4    |              DDR5              |
| :-----: | :-------: | :----------------------------: |
|  VDDQ   |   1.2V    |              1.1V              |
|   预读取   |    8n     |              16n               |
| DIMM 通道 |  单 64bit  |       双 32bit（突发长度 16→8）       |
|  数据速率   | 1600~3200 |           3200~6400            |
|  每芯片容量  |   16Gb    |              64Gb              |
|  PMIC   |   主板供电    |     DIMM 自带 PMIC（12V→多路电压）     |
|   SPD   |    基础     |       SPD Hub（I²C 总线管理）        |
|   ECC   |    部分     |       On-Die ECC（每芯片内部）        |
|   BL    |     8     | BL16（但 32bit 通道 = 传统 64bit 一半） |

### DDR5 双通道设计

```
DDR4: 一条 DIMM = 64bit DQ（ECC 版 72bit）
DDR5: 一条 DIMM = 2 × 32bit 子通道 = 64bit

       Channel A (32bit)         Channel B (32bit)
       ┌────────────────┐      ┌────────────────┐
       │  DQ[31:0]       │      │  DQ[63:32]      │
       │  DQS_A[3:0]     │      │  DQS_B[3:0]     │
       │  CA_A[6:0]      │      │  CA_B[6:0]      │
       │  CKE_A, CS_A    │      │  CKE_B, CS_B    │
       └────────────────┘      └────────────────┘
```

两个子通道完全独立（独立命令/地址/时钟），PHY 面积约等于 DDR4 的 1.4 倍。

---

## 六、Xilinx 7 系列 + MIG IP

### MIG 概述

MIG (Memory Interface Generator) = Xilinx 官方 DDR 控制器 IP，支持 DDR2/DDR3/LPDDR。

```
FPGA Fabric
┌────────────────────────────────────────────────┐
│     User Interface                              │
│  ┌────────────────────┐                         │
│  │ app_wdf_data       │ ───→                   │
│  │ app_addr           │                        │
│  │ app_cmd            │                         │
│  │ app_en / app_rdy   │ ←→                     │
│  └────────┬───────────┘                         │
│           │                                    │
│  ┌────────▼───────────┐                         │
│  │    Memory Controller │                        │
│  │  - Command Queue    │                        │
│  │  - Bank Management  │                         │
│  │  - Refresh          │                        │
│  └────────┬───────────┘                         │
│           │                                    │
│  ┌────────▼───────────┐                         │
│  │    PHY              │                         │
│  │  - DQ/DQS IOB      │                        │
│  │  - DQS gate        │                         │
│  │  - Write levelling  │                        │
│  │  - Read training    │                        │
│  └────────┬───────────┘                         │
└───────────┼────────────────────────────────────┘
            │
            ▼
      FPGA IOB (HP bank / HR bank)
            │
            ▼
      DDR3/DDR4 芯片
```

### XC7A35T 的 MIG 限制

| 参数 | XC7A35T (HR bank) |
|:----:|:------------------:|
| 最大速率 | DDR3 1066 Mbps (533 MHz) |
| DQ 位宽 | 最大 16bit（1 个字节组） |
| 可用 bank | HR bank（1.35V/1.5V SSTL） |
| 不支持 | DDR4（7 系列不支持 DDR4） |
| 不支持 | 硬 PHY 校准逻辑（需 MIG 软核） |

XC7S 系列（Spartan-7）类似，XC7K 和 XC7V（Kintex-7 / Virtex-7）支持 HP bank（DDR3-1866+）。

### MIG 配置要点

```
Clock Period:  3000 ps  → 333.33 MHz → DDR3-667 (不满足)
               1875 ps  → 533.33 MHz → DDR3-1066 ← XC7A35T 最大值
               1500 ps  → 666.67 MHz → DDR3-1333 (需 HP bank)

Memory Part:   MT41J128M16xx-125 (128M16, 2Gb, 800MHz/1066MHz)
Data Width:    16
Data Mask:     Enable
Burst Length:  8
CAS Latency:   取决于芯片型号和速率

System Clock:  差分/单端 50MHz（ACX720 板载 50MHz 单端）
Reference Clock: 200MHz（MIG 内部 PLL/MMCM）

Output Driver Impedance Control: RZQ/6 (40Ω) / RZQ/7 (34Ω)
ODT: RZQ/4 (60Ω)
```

### MIG 用户接口（Native 接口）

|             信号             | 方向  | 说明                                    |
| :------------------------: | :-: | ------------------------------------- |
|       app_addr[28:0]       |  →  | 用户地址（行+列+bank 低位拼接）                   |
|        app_cmd[2:0]        |  →  | 命令: 000=写, 001=读                      |
|           app_en           |  →  | 命令使能                                  |
|          app_rdy           |  ←  | 控制器就绪（握手，app_en 有效且 app_rdy=1 时命令被接收） |
| app_wdf_data[2×DWIDTH-1:0] |  →  | 写数据（2× 位宽 = 32bit 对于 DQ=16）           |
|        app_wdf_wren        |  →  | 写数据使能                                 |
|        app_wdf_end         |  →  | 最后一个写数据拍                              |
|        app_wdf_mask        |  →  | 写掩码                                   |
|        app_wdf_rdy         |  ←  | 写数据 FIFO 就绪                           |
|        app_rd_data         |  ←  | 读数据                                   |
|     app_rd_data_valid      |  ←  | 读数据有效                                 |
|      app_rd_data_end       |  ←  | 最后一个读数据                               |

### Native 接口写时序

```
CLK     ┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐┌┐
            │  │  │  │  │  │  │  │  │  │  │
app_en  ────┤██├─────────────────────────
            └──┘
app_rdy ─────────┤██├██├─────────────────  ← 命令在 app_en & app_rdy 时接收
                  └──┘──┘
app_cmd ────┤001├─────────────────────────
            └──┘
app_addr────┤addr├────────────────────────
            └────┘
app_wdf_wren ───────┤████├──── 写数据与命令可错拍
                    └────┘
app_wdf_data ───────███├──── 写入的数据
                    └──┘
```

### AXI4 接口（MIG 也支持）

MIG 可选择 AXI4 接口代替 Native：

```verilog
// AXI4 写
axi_awaddr  → 写地址
axi_awvalid → 写地址有效
axi_awready → 目标就绪
axi_wdata   → 写数据
axi_wvalid  → 写数据有效
axi_wready  → 写数据就绪
axi_wlast   → 最后一拍
axi_bresp   → 写响应
axi_bvalid  → 响应有效
```

---

## 七、ACX720 外接 DDR3

ACX720 板载一片 **DDR3 SDRAM**（MT41J128M16 或兼容型号）：

| 参数 | 值 |
|:----:|:---:|
| 型号 | MT41J128M16HA-125 |
| 组织 | 128Meg × 16bit = 2 Gbit (256 MB) |
| 速率 | DDR3-1066 (CL=7) / DDR3-800 (CL=6) |
| DQ | 16bit |
| Bank | 8 (BA[2:0]) |
| Row | 14 bit |
| Column | 10 bit |
| 封装 | BGA96 |

### 引脚连接

| FPGA (XC7A35T) | DDR3 | Bank |
|:--------------:|:----:|:----:|
| I/O | DQ[15:0] | HR (B13/B14) |
| I/O | DQS_P/N[1:0] | HR |
| I/O | A[13:0], BA[2:0] | HR |
| I/O | /CS, /RAS, /CAS, /WE | HR |
| I/O | CK_P/N | HR (差分时钟输出) |
| I/O | CKE, ODT | HR |
| I/O | DM[1:0] | HR |
| I/O | ZQ_HR (240Ω 下拉) | HR |

### 关键设计考虑

1. **DQS 延时校准**：MIG 内部通过 IDELAYE2 的 tap 调整读 DQS 相位
2. **Write Levelling**：补偿 CK 与 DQS 的走线偏差
3. **Read Training**：调整读 DQS 门控（gate）窗口
4. **PCB 走线等长**：DQ/DQS 组内 ±1ps，CK 到 DQS ±20ps
5. **参考时钟**：MIG 需要独立的 200MHz 参考时钟（可用 MMCM 生成）

---

## 八、FPGA DDR 设计流程

### 步骤

```
1. 确认 DDR 器件型号 → 查看数据手册
2. Vivado → IP Catalog → MIG 7 Series
3. 选择 Memory Part 或手动输入参数
4. 配置时钟（系统时钟 = 板载 50MHz）
5. 配置 PHY（DQ/DQS/DM 引脚分配）
6. 选择用户接口（Native / AXI4）
7. 生成 bitstream → 验证 Initialization Calibration Done
8. 读写测试（Vivado ILA + Data Pattern Generator）
```

### 测试模板

```verilog
// MIG Native 接口写测试
reg [28:0]   addr;
reg [2:0]    cmd;
reg          en;
reg [31:0]   wdf_data;
reg          wdf_wren;

always @(posedge clk) begin
    if (rst_n && init_calib_complete) begin
        if (app_rdy && en) begin
            addr <= addr + 1;
        end
        if (app_wdf_rdy && wdf_wren) begin
            wdf_data <= wdf_data + 1;
        end
    end
end

// 启动写任务
assign cmd = 3'b000;  // 写
assign en  = write_active && (addr < end_addr);
assign wdf_wren = en;  // 命令与写数据同拍
assign wdf_data  = {16'hA5A5, 16'h5A5A};
```

---

## 九、DDR 调试

### 使用 Vivado 调试 MIG

1. **Init Calibration Done**：MIG 输出 `init_calib_complete`，为 1 才可访问
2. **Vivado ILA**：抓取 app_rdy / app_en 握手确认命令是否被控制器接收
3. **Data Compare**：写已知 Pattern → 读回比较
4. **调整 IDELAY**：如果读数据随温度漂移，可动态调整 DQS 相位

### 常见问题

| 现象 | 原因 | 解决 |
|:----|------|------|
| init_calib_complete 一直为 0 | PHY 校准失败 | 检查 DQS/DQ 引脚分配、时钟频率、DDR 芯片型号 |
| 写不等，读永远不对 | Bank 管理问题 | MIG 自动管理 Bank，检查 app_cmd 是否正确 |
| 读数据有时正确有时错误 | 时序边际不足 | 放宽 MIG 时钟频率，增加 IDELAY 微调 |
| 高温下 bit error | 温度漂移 | 使用 DQS 自动漂移补偿，加散热 |
| DDR3-1066 跑不到 1066 | HR bank 速率受限 | 降速到 800 Mbps 或换 HP bank 器件 |

---

## 十、DDR vs FPGA 内部 RAM

| 特性 | Block RAM (BRAM) | Distributed RAM (LUTRAM) | DDR3 (外部) |
|:----:|:----------------:|:------------------------:|:-----------:|
| 容量 | 36 Kb/块 | ~64bit/LUT | 256 MB+ |
| 延迟 | 1~3 clk | 1 clk | 20~50 clk |
| 带宽 | ~10 GB/s | ~50 GB/s | ~2 GB/s (DDR3-800 16bit) |
| 资源消耗 | 专用 | 逻辑资源 | IO + PHY + 控制器 |
| 适合 | FIFO, 小缓存 | 寄存器文件 | 大容量存储 (帧缓存) |

---

## 参考

- JEDEC JESD79-3F (DDR3)
- JEDEC JESD79-4C (DDR4)
- JEDEC JESD79-5 (DDR5)
- Xilinx UG586: MIG 7 Series User Guide
- Xilinx UG471: 7 Series SelectIO Resources
- MT41J128M16 数据手册
- [[24HDMI 显示驱动详解]] — DDR 作为帧缓冲用于 HDMI 输出
- [[21FPGA FIFO 详解]] — DDR 与 FIFO 配合实现跨时钟域大数据流
- [[28CAN通信协议详解]] — 不同场景的存储需求对比
