---
tags:
  - FPGA
  - I2C
  - 通信协议
  - 串行总线
---

## 核心结论

- **I²C**（Inter-Integrated Circuit）= 两线制同步串行总线，由 Philips 开发
- **两线**：SCL（时钟）+ SDA（数据），均为**开漏输出**，需上拉电阻
- **多主多从**：每个设备有唯一 7 位地址，通过仲裁决定哪个主设备控制总线
- **速度**：标准 100kHz，快速 400kHz，高速 3.4MHz
- FPGA 通常实现为 **I²C Master**，控制与 EEPROM / 传感器 / ADC / 屏幕等从设备的通信

---

## 一、四大串行协议对比

| 特性 | I²C | SPI | USART (UART) | CAN |
|:----:|:---:|:---:|:------------:|:---:|
| 线数 | 2（SCL + SDA） | 4（SCLK + MOSI + MISO + CS） | 2（TX + RX） | 2（CAN_H + CAN_L） |
| 同步/异步 | 同步（有 SCL） | 同步（有 SCLK） | 异步（无时钟线） | 异步（差分） |
| 信号类型 | 单端开漏 | 单端推挽 | 单端推挽 | 差分（CAN_H/CAN_L） |
| 拓扑 | 总线型（多设备共享） | 星型（一主多从，各需 CS） | 点对点 | 总线型（多节点） |
| 寻址方式 | 7 位/10 位地址 | 片选 CS 引脚 | 无（需应用层约定） | 11 位/29 位 ID + 仲裁 |
| 最大速率 | 3.4 MHz（高速） | ~50 MHz+ | ~1 Mbps（典型） | 1 Mbps（CAN 2.0） |
| 最大距离 | ~1m（板级） | ~1m（板级） | ~15m（RS232），~1km+（RS485） | ~1km+ |
| 多主能力 | ✅ 支持（仲裁） | ❌ 通常一主 | ❌ 点对点 | ✅ 多主（CSMA/CR） |
| 流控 | 时钟拉伸（从设备控制速度） | 无硬件流控 | RTS/CTS（可选） | 错误帧/重传（硬件） |
| 错误检测 | ACK/NACK | 无硬件校验 | 奇偶校验（可选） | CRC + 错误帧 + 自动重传 |
| 硬件复杂度 | 低（2 线上拉） | 中（每从多一条 CS） | 低 | 高（需 CAN 收发器） |
| FPGA 实现难度 | 中（SCL 时序 + 状态机） | 低（移位寄存器） | 低（波特率计数） | 高（需 CAN 控制器 IP） |
| 典型场景 | EEPROM、传感器、OLED | ADC、DAC、SD 卡、Flash | 调试串口、GPS、蓝牙 | 汽车电子、工业现场 |

### 选型速查

| 需求 | 推荐协议 | 原因 |
|------|---------|------|
| 板内接 EEPROM / 传感器 | I²C | 两线即可挂载多个设备 |
| 高速连续数据（ADC/摄像头） | SPI | 速率高，无协议开销 |
| PC 调试 / 低速无线模块 | USART | 简单通用，PC 原生支持 |
| 工业现场 / 车载通信 | CAN | 差分远距离，强错误处理 |
| 大量从设备（>10 个） | I²C / CAN | 总线式，不占用额外引脚 |
| 高实时性控制 | SPI / CAN | 低延迟确定通信 |

---

## 二、I²C 物理层

### 总线连接

```
VDD                    VDD                    VDD
  ├─ Rp ─┐              ├─ Rp ─┐              ├─ Rp ─┐
  │      │              │      │              │      │
SCL ─────┼─────── SCL ──┼────── SCL ──────────┼── ──┤
  │      │              │      │              │     OD
SDA ─────┼─────── SDA ──┼────── SDA ──────────┼── ──┤
         │              │      │              │     OD
      Master           Slave1              Slave2
```

- **开漏输出**：设备只能拉低，不能拉高（高电平由上拉电阻实现）
- **上拉电阻**：标准 4.7kΩ（100kHz）/ 2.2kΩ（400kHz）
- **线与特性**：任一设备拉低，总线即低；所有设备释放，总线才高

### 逻辑电平

| 标准 | VDD | VIH(min) | VIL(max) |
|:----:|:---:|:--------:|:--------:|
| 3.3V I²C | 3.3V | 2.31V | 0.99V |
| 5V I²C | 5.0V | 3.5V | 1.5V |

FPGA 的 LVCMOS33 引脚可直接驱动 3.3V I²C 总线（需外接上拉电阻）。

---

## 三、I²C 协议层

### 时序图

```
SCL ﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍
         ┌──┐   ┌──┐   ┌──┐   ┌──┐   ┌──┐   ┌──┐
SDA ﹍﹍﹍│  │﹍﹍│  │﹍﹍│  │﹍﹍│  │﹍﹍│  │﹍﹍│
         └──┘   └──┘   └──┘   └──┘   └──┘   └──┘
        START            bit7   bit6   ...   ACK  STOP
```

### Start 条件

```
SCL  ﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉\____________
SDA  ﹉﹉﹉﹉﹉﹉\_______________/﹉﹉﹉﹉﹉
              ↑
          SCL 高时 SDA 由高→低
```

### Stop 条件

```
SCL  ____________/﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉﹉
SDA  ﹉﹉﹉﹉﹉﹉\_______________/﹉﹉﹉﹉
                                       ↑
                                  SCL 高时 SDA 由低→高
```

### 数据帧格式

```
  START | 7bit 地址 | R/W | ACK | 8bit 数据 | ACK | ... | 8bit 数据 | ACK | STOP
         └── 1 字节 ──┘     └── 1 字节 ──┘         └── 1 字节 ──┘
```

|   字段    | 说明                          |
| :-----: | --------------------------- |
|  START  | SCL 高时 SDA 下降沿              |
| 7bit 地址 | 从设备地址（高位先发）                 |
|   R/W   | 1 = 读，0 = 写                 |
|   ACK   | 第 9 个 SCL 周期：接收方拉低 SDA 表示应答 |
| 8bit 数据 | 每字节 MSB first               |
|  STOP   | SCL 高时 SDA 上升沿              |

### ACK / NACK

```
SCL  ﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍￣﹍﹍￣
                   ┌──┐
SDA  ──────────────│  │──────────  ← Master 释放 SDA
                   └──┘
                   ↑
              ACK: Slave 拉低 SDA
              NACK: Slave 不拉（或 Master 不拉）= 高
```

- **写操作**：每个字节后 Master 释放 SDA，Slave 拉低 ACK
- **读操作**：每个字节后 Master 拉低 ACK（表示还要继续读），或释放=NACK（最后字节，通知停止）

### 完整写操作示例（写 EEPROM 一个字节）

```
Master: START | 0xA0 (W) | ACK← | 0x00 (地址高) | ACK← | 0x10 (地址低) | ACK← | 0x55 (数据) | ACK← | STOP
          ①        ②      ③         ④             ⑤        ⑥           ⑦       ⑧          ⑨    ⑩
```

1. Master 发 START
2. Master 发设备地址 0x50<<1 + W(0) = 0xA0
3. Slave 拉低 ACK
4. Master 发内部地址高字节
5. Slave ACK
6. Master 发内部地址低字节
7. Slave ACK
8. Master 发写入数据
9. Slave ACK
10. Master 发 STOP

### 完整读操作示例（从 EEPROM 读取一个字节）

```
Master: START | 0xA0 (W) | ACK← | 地址H | ACK← | 地址L  | ACK← | 
        START | 0xA1 (R) | ACK← | ←数据  | NACK→ | STOP
```

写地址 → 重启 → 读数据，称为**复合传输 (Combined Transaction)**。

---

## 四、FPGA 实现 I²C Master

### 模块架构

```
                  ┌───────────────────┐
  clk ──────────→│    时钟分频       │──→ SCL 时钟（tick）
                  └───────────────────┘
                          │
                  ┌───────┴───────┐
                  │  I2C 状态机   │
                  │               │
                  │ IDLE → START │
                  │ → SEND_ADDR  │
                  │ → SEND_DATA  │
                  │ → STOP       │
                  └───────┬───────┘
                          │
              ┌───────────┴───────────┐
              │  SCL / SDA 控制       │
              │  （开漏模拟 + 时序）   │
              └───────────────────────┘
```

### 状态机

```
                START
                  │
                  ▼
              SEND_ADDR ──→ ACK? ──→ NACK → STOP
                  │ YES
                  ▼
              SEND_REG_ADDR ──→ ACK? ──→ NACK → STOP
                  │ YES
          ┌───────┴───────┐
          │               │
      SEND_DATA        RESTART → SEND_ADDR_R
     (写操作)           (读操作)
          │               │
          ▼               ▼
         ACK?          SEND_ADDR_R ──→ ACK?
          │               │ YES
          ▼               ▼
         STOP          READ_DATA ──→ Master ACK/NACK
                            │
                            ▼
                           STOP
```

### 核心代码

```verilog
module i2c_master (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        start,        // 触发一次传输
    input  wire        rw,           // 1=读, 0=写
    input  wire [6:0]  dev_addr,     // 从设备 7bit 地址
    input  wire [7:0]  reg_addr,     // 寄存器/内部地址
    input  wire [7:0]  wdata,        // 写入数据
    output reg  [7:0]  rdata,        // 读回数据
    output reg         done,         // 传输完成
    output reg         busy,         // 忙标志
    inout              scl,          // I²C 时钟
    inout              sda           // I²C 数据
);
    parameter CLK_DIV = 50_000_000 / (400_000 * 4) - 1;  // 400kHz SCL

    // I²C 开漏驱动：输出 0 拉低，输出 1 高阻（由上拉电阻拉高）
    reg scl_out, sda_out;
    assign scl = scl_out ? 1'bz : 1'b0;
    assign sda = sda_out ? 1'bz : 1'b0;
    wire sda_in = sda;  // 读 SDA 电平（判断 ACK）

    // 4x 过采样状态机（每个 SCL 周期 4 个节拍）
    reg [1:0] phase;     // 0=SCL低前半, 1=SCL低后半, 2=SCL高前半, 3=SCL高后半
    reg [3:0] bit_cnt;   // 当前 bit 索引 (0~8, 9=ACK)
    reg [7:0] sh_reg;    // 移位寄存器
    reg [3:0] state;

    localparam IDLE      = 0;
    localparam GEN_START = 1;
    localparam SEND_BYTE = 2;
    localparam GET_ACK   = 3;
    localparam GEN_STOP  = 4;
    localparam FINISH    = 5;

    always @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            state <= IDLE;
            scl_out <= 1'b1;
            sda_out <= 1'b1;
            done <= 1'b0;
            busy <= 1'b0;
        end else case (state)
            IDLE: begin
                scl_out <= 1'b1;  // 空闲：SCL 高
                sda_out <= 1'b1;  // SDA 高
                done <= 1'b0;
                if (start && !busy) begin
                    busy <= 1'b1;
                    sh_reg <= {dev_addr, rw};  // 7bit 地址 + R/W
                    bit_cnt <= 8;
                    state <= GEN_START;
                end
            end

            GEN_START: begin
                scl_out <= 1'b1;
                sda_out <= 1'b0;  // SCL 高时 SDA 下降沿 = START
                state <= SEND_BYTE;
            end

            SEND_BYTE: begin
                if (phase == 0) begin
                    scl_out <= 1'b0;     // SCL 拉低
                    sda_out <= sh_reg[7]; // 设置数据（MSB first）
                    sh_reg  <= {sh_reg[6:0], 1'b0};
                end else if (phase == 2) begin
                    scl_out <= 1'b1;     // SCL 拉高，从设备捕获数据
                end else if (phase == 3) begin
                    if (bit_cnt > 0) bit_cnt <= bit_cnt - 1;
                    else state <= GET_ACK;  // 8 bit 发送完，等 ACK
                end
            end

            GET_ACK: begin
                if (phase == 0) begin
                    scl_out <= 1'b0;
                    sda_out <= 1'b1;    // 释放 SDA（从设备拉低表示 ACK）
                end else if (phase == 2) begin
                    scl_out <= 1'b1;
                end else if (phase == 3) begin
                    // 检查 ACK
                    if (!sda_in) begin
                        // ACK 收到
                        if (rw) begin
                            // 读操作模式
                            sh_reg <= 8'h00;
                            bit_cnt <= 8;
                            // 发送 RESTART + 读地址
                        end else begin
                            // 写完成
                            state <= GEN_STOP;
                        end
                    end else begin
                        state <= GEN_STOP;  // NACK，停止
                    end
                end
            end

            GEN_STOP: begin
                if (phase == 0) begin
                    scl_out <= 1'b0;
                    sda_out <= 1'b0;
                end else if (phase == 2) begin
                    scl_out <= 1'b1;     // SCL 先高
                end else if (phase == 3) begin
                    sda_out <= 1'b1;     // SCL 高时 SDA 上升沿 = STOP
                    state <= FINISH;
                end
            end

            FINISH: begin
                if (phase == 3) begin
                    scl_out <= 1'b1;
                    sda_out <= 1'b1;
                    busy <= 1'b0;
                    done <= 1'b1;
                    state <= IDLE;
                end
            end
        endcase
    end

endmodule
```

> 以上代码为框架示例，完整实现需要处理：时钟分频、phase 计数器、读时序（RESTART + 读数据 + Master ACK/NACK）、超时保护等。

---

## 五、常见 I²C 设备

### EEPROM（AT24Cxx 系列）

| 参数 | 值 |
|:----:|:---:|
| 容量 | 1Kbit ~ 2Mbit |
| 地址 | 3 位硬件引脚（A0/A1/A2）→ 最多 8 片共享总线 |
| 页写 | 连续写 8~64 字节（视型号），无需每字节 STOP |
| 写周期 | 每个页写后需等待 5ms（FPGA 需延时再发） |

### 传感器

| 设备 | 类型 | 地址 | 特点 |
|:----:|:----:|:----:|------|
| LM75 | 温度 | 0x48~0x4F | 9bit 精度，±2°C |
| MPU6050 | 6 轴 IMU | 0x68 | 陀螺仪 + 加速度计 |
| BME280 | 环境 | 0x76/0x77 | 温湿度 + 气压 |
| ADXL345 | 加速度 | 0x53 | 3 轴，±16g |

### OLED 屏幕（SSD1306）

| 参数 | 值 |
|:----:|:---:|
| 分辨率 | 128×64 |
| 地址 | 0x3C (写) / 0x3D (读) |
| 驱动方式 | 写命令 → 写显存数据（GDDRAM） |
| 速率 | I²C 模式最高 400kHz，更新~15fps |

---

## 六、I²C 调试常见问题

### 1. SCL / SDA 一直高

- 上拉电阻未焊接或虚焊
- FPGA 的 sda_out / scl_out 未正确初始化为 1'b1

### 2. 一直收到 NACK

- 设备地址错误（注意 7bit 地址左移 1 位才是发送的字节）
- 设备未上电
- SCL 频率超过设备支持的最大速率
- 总线被其他设备拉死

### 3. SDA 被拉死（一直低）

- 设备在 ACK 后等待数据但主设备 STOP 了
- 主设备发完数据后未释放 SDA 就发 STOP
- 解决方案：发 9 个脉冲 + STOP 复位从设备

### 4. FPGA 驱动能力

FPGA 的普通 IO 推挽输出可直接驱动 I²C（设为 OD 模式），但需确认：
- 上拉电阻值合适（根据总线电容和频率）
- 引脚 IOSTANDARD 与 VDD 匹配

---

## 七、与已有笔记的关联

| 笔记 | 关联点 |
|------|--------|
| [[17SPI通信与ADC128S102驱动设计]] | SPI vs I²C 详细对比，SCLK 与 SCL 区别 |
| [[14UART串口发送模块设计与验证]] | UART（异步）vs SPI/I²C（同步）的根本区别 |
| [[15UART串口接收模块设计与验证]] | 同上 |
| [[24HDMI 显示驱动详解]] | SSD1306 OLED 可用 I²C 驱动，与 HDMI 直驱对比 |

---

## 八、扩展：CAN 协议要点

### CAN 与 I²C 的核心差异

| 特性 | I²C | CAN |
|:----:|:---:|:---:|
| 物理层 | 单端开漏，共地 | 差分（CAN_H / CAN_L），隔离 |
| 电平 | 0 / VDD | 显性 (0) / 隐性 (1)，差分 ~1.5V |
| 最大距离 | ~1m | ~1km（125kbps），~40m（1Mbps） |
| 最大节点 | ~128（负载电容限制） | ~110（标准帧） |
| 仲裁 | 多主，SCL/SDA 线与 | CSMA/CR，按 ID 优先级 |
| 错误处理 | ACK/NACK | CRC + 错误帧 + 自动重传 |
| 数据帧 | 每字节 + ACK | 2.0A: 8 字节/帧，2.0B: 8 字节/帧，FD: 64 字节 |
| FPGA 实现 | 状态机 + GPIO | 需 CAN 控制器 IP（如 Xilinx CAN），需外部 CAN 收发器（如 TJA1050） |

### CAN 帧格式（标准帧 2.0A）

```
SOF | 11bit ID | RTR | IDE | r0 | DLC | 0~8 字节数据 | CRC | ACK | EOF
```

### FPGA 注意事项

- **不推荐用 FPGA 纯逻辑实现 CAN**：时序要求严格，错误处理复杂
- 工业常用方案：**MCU（带 CAN 外设）+ FPGA**，MCU 做 CAN 网关，FPGA 做数据处理
- 若必须在 FPGA 实现，使用 Xilinx CAN IP 核或开源 CAN 控制器（如 SJA1000 兼容）

---

## 九、四大协议总结

```
速度:           USART(慢) < I²C(中) < SPI(快) < CAN(中高速)
距离:           I²C(板级) < SPI(板级) < USART(中) < CAN(远)
线数:           SPI(4+) > CAN(2+收发器) > I²C(2) = USART(2)
多设备:         CAN(多主) > I²C(多主) > SPI(一主多从) > USART(点对点)
错误处理:       CAN(强) > I²C(ACK) > USART(奇偶) > SPI(无)
开发难度(FPGA): CAN(高) > I²C(中) > SPI(低) = USART(低)
```

---

## 参考

- NXP UM10204: I²C-bus specification and user manual
- Xilinx UG471: 7 Series SelectIO Resources
- AT24Cxx 数据手册
- SSD1306 数据手册
- CAN 2.0B 规范
- [[17SPI通信与ADC128S102驱动设计]]
