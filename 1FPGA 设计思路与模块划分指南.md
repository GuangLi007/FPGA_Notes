---
tags:
  - FPGA
---


## 一、FPGA 设计的一般流程

```
需求分析 → 功能划分 → 模块设计 → 顶层集成 → 仿真验证 → 综合实现 → 板级调试
```

---

## 二、模块划分原则

### 2.1 按功能划分

| 模块类型 | 功能 | 示例 |
|---------|------|------|
| **时钟/复位模块** | 产生各模块需要的时钟 | 分频器、PLL |
| **输入模块** | 处理外部输入信号 | 按键消抖、键盘扫描 |
| **控制模块** | 核心控制逻辑 | 状态机、协议解析 |
| **输出模块** | 驱动外部设备 | 数码管显示、蜂鸣器、LED |
| **通信模块** | 与外部芯片通信 | UART、I2C、SPI |

### 2.2 划分原则

| 原则 | 说明 |
|------|------|
| **高内聚** | 相关功能放在同一模块 |
| **低耦合** | 模块间接口简单清晰 |
| **可复用** | 通用功能独立成模块 |
| **可测试** | 每个模块可独立仿真 |

---

## 三、电梯项目模块划分示例

### 3.1 模块结构图

```
                          ┌─────────────────┐
                          │     top.v       │
                          │   (顶层模块)     │
                          └────────┬────────┘
                                   │
         ┌──────────────┬──────────┼──────────┬──────────────┐
         │              │          │          │              │
         ▼              ▼          ▼          ▼              ▼
┌─────────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│    s_div        │ │keypad_   │ │  Beep    │ │decoder_  │ │ 电梯     │
│  (时钟分频)     │ │scanner   │ │(蜂鸣器)  │ │7seg      │ │状态机    │
│                 │ │(键盘扫描) │ │          │ │(数码管)  │ │          │
└─────────────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘
```

### 3.2 模块功能描述

| 模块 | 功能 | 输入 | 输出 |
|------|------|------|------|
| **s_div** | 时钟分频 | 50MHz 时钟 | 100ms、20ms、2ms、1kHz |
| **keypad_scanner** | 4x4键盘扫描 | 列输入 | 按键状态 |
| **Beep** | 电梯到达提示 | 1kHz时钟、到达信号 | 方波输出 |
| **decoder_7seg** | 数码管显示 | 位置、状态 | 段选、位选 |
| **电梯状态机** | 核心控制 | 按键请求 | 位置、状态、LED |

---

## 四、顶层模块设计

### 4.1 顶层模块的作用

| 作用 | 说明 |
|------|------|
| **例化子模块** | 将所有子模块组装起来 |
| **连接信号** | 定义子模块间的连线 |
| **定义顶层端口** | 芯片的物理引脚 |
| **分配时钟/复位** | 全局信号分发 |

### 4.2 顶层模块模板

```verilog
module top(
    // 时钟和复位
    input clk,
    input rst_n,
    
    // 输入接口
    input [3:0] col,
    
    // 输出接口
    output [3:0] row,
    output [7:0] seg,
    output [5:0] dig,
    output beep,
    output reg [11:0] led
);

    //=========================================
    // 内部信号声明（导线）
    //=========================================
    wire clk_100ms;      // 状态机时钟
    wire clk_btn;        // 按键扫描时钟
    wire clk_seg;        // 数码管扫描时钟
    wire clk_beep;       // 蜂鸣器时钟
    wire [15:0] key_out; // 按键状态
    wire [2:0] state;    // 电梯状态
    // ... 其他信号

    //=========================================
    // 子模块例化
    //=========================================
    
    // 时钟分频模块
    s_div u_s_div(
        .clkin(clk),
        .clkout(clk_100ms),
        .clkout2(clk_btn),
        .clkout3(clk_seg),
        .clkout4(clk_beep)
    );
    
    // 键盘扫描模块
    keypad_scanner u_scanner(
        .clk(clk_btn),
        .col(col),
        .row(row),
        .key_state(key_out)
    );
    
    // 蜂鸣器模块
    Beep u_beep(
        .clk_beep(clk_beep),
        .reset(rst_n),
        .Elevator_State(state == DOOR_OPEN),
        .Enable(power),
        .beep(beep)
    );
    
    // 数码管模块
    decoder_7seg u_decoder(
        .clkin(clk),
        .power(power),
        .pos(pos),
        .move_cnt(move_cnt),
        .state(state),
        .seg(seg),
        .dig(dig)
    );
    
    // 电梯状态机（直接在顶层实现）
    always @(posedge clk_100ms or negedge rst_n) begin
        // 状态机逻辑
    end

endmodule
```

---

## 五、设计注意事项

### 5.1 时钟管理

| 要点 | 说明 |
|------|------|
| **单一时钟源** | 所有模块使用同一 PLL 或分频器 |
| **时钟命名** | `clk_xxx` 格式，清晰标识频率 |
| **避免门控时钟** | 使用时钟使能而非门控 |

### 5.2 复位策略

| 要点 | 说明 |
|------|------|
| **同步复位** | 推荐，避免亚稳态 |
| **异步复位** | 简单，注意释放时序 |
| **复位信号名称** | `rst_n`（低有效）或 `rst`（高有效） |

### 5.3 信号命名规范

| 信号类型 | 命名格式 | 示例 |
|---------|---------|------|
| 时钟 | `clk_xxx` | `clk_100ms` |
| 复位 | `rst_n` | `rst_n` |
| 低有效 | `xxx_n` | `enable_n` |
| 状态 | `state` 或 `current_state` | `state` |
| 计数器 | `xxx_cnt` | `wait_cnt` |

---

## 六、设计检查清单

### 6.1 模块设计检查

- [ ] 功能是否单一清晰？
- [ ] 接口是否简单合理？
- [ ] 是否有独立的仿真文件？

### 6.2 顶层集成检查

- [ ] 所有子模块是否已例化？
- [ ] 内部信号类型是否正确（wire/reg）？
- [ ] 未使用的输入是否已处理？
- [ ] 时钟/复位是否正确分发？

### 6.3 综合前检查

- [ ] 是否有未连接的端口？
- [ ] 是否有组合逻辑环路？
- [ ] 跨时钟域信号是否处理？

---

## 七、设计原则总结

| 原则 | 说明 |
|------|------|
| **自顶向下** | 先设计顶层框架，再细化子模块 |
| **模块化** | 每个模块功能单一、接口清晰 |
| **可读性** | 代码注释、命名规范、结构清晰 |
| **可移植性** | 避免与具体芯片绑定的硬编码 |
| **可测试性** | 每个模块可独立仿真验证 |
```