# Segment_Display_1 数码管显示 — 工程记录

## 概述

尝试在 ACX720 开发板上驱动 8 位数码管，经历了从"想当然直驱"到"认清硬件用 HC595"再到"对比 GPIO 直驱模板"的完整过程。

## 硬件真相

ACX720 板载数码管**不是 GPIO 直驱**，而是通过**两片级联的 74HC595** 驱动：

| 信号 | FPGA 引脚 | 说明 |
|------|-----------|------|
| `ds` (SEG7_DIO) | M18 | 串行数据 |
| `sh_cp` (SEG7_SCLK) | F4 | 移位时钟 |
| `st_cp` (SEG7_RCLK) | C2 | 锁存时钟 |

即 FPGA 只需 **3 根线** 控制 8 位数码管，而非 16 根。

## 踩坑记录

### 坑 1：直接写了 GPIO 直驱代码

看到参考模块有 `seg[7:0]` 和 `sel[7:0]` 就以为可以直接这么用，忽略了板级原理图。

**教训**：写代码前先查原理图或管脚分配表，确认硬件接口类型。

### 坑 2：位选 SEL 极性试错

一开始用 `SEL = 8'b1111_1110`（低电平选中），结果只亮一位。通过对比参考工程发现 **ACX720 的 HC595 位选是高电平有效** (`8'b0000_0001`)。

**教训**：极性不确定时，先用"全亮"调试模式定位问题。

## 两个方案对比

| 项目 | HC595 串行驱动 | GPIO 并行直驱 |
|------|---------------|--------------|
| FPGA 引脚占用 | 3 根 | 16 根 |
| 代码复杂度 | 需 driver + control 两个模块 | 一个模块搞定 |
| 驱动速度 | 需串行时钟，约 680ns/帧 | 并行输出，无额外延迟 |
| 适用场景 | 引脚紧张、板载设计 | 引脚充足、外接模块 |
| 学习价值 | 理解串并转换、级联、时序 | 理解动态扫描、段码译码 |

## 文件结构

```markdown
Segment_Display_1/
├── Top.v           # 单文件包含 3 个模块
│   ├── HEX8           # 顶层
│   ├── Seg_Control    # 分频 + 扫描 + 译码
│   └── Hc595_Driver   # 595 串行驱动
└── Cons.xdc        # 引脚约束
```

## GPIO 直驱模板（参考用）

若将来使用 GPIO2 外接数码管模块，可直接套用 `HEX8_GPIO` 模块。其与 HC595 版的核心差异在**输出端口**：

```verilog
// HC595 版
module HEX8 (clk, reset_n, sw, ds, sh_cp, st_cp);

// GPIO 直驱版
module HEX8_GPIO (clk, reset_n, sw, seg, sel);
```

其余扫描逻辑和段码表完全复用。

## 关键知识点

- **动态扫描**：人眼余晖 ~20ms，8 位 × 1ms = 8ms，刷新率 125Hz
- **段码译码**：共阳极 0=亮，共阴极 1=亮，不可混用
- **HC595 级联**：Q7' 接下一片 SER，先移入的数据到后级
- **持续赋值 vs 过程赋值**：`output reg` 不能用 `assign`，会报 `concurrent assignment to a non-net`

## 验证结果

| 操作 | 现象 |
|------|------|
| 烧录后默认 | 显示 `123456` + 拨码开关值 |
| 拨动 SW[3:0] | 第 1 位数字变化 |
| 拨动 SW[7:4] | 第 2 位数字变化 |

## 参考链接

- 参考工程：`/home/kl/My_Project/FPGA/Segment_Display/`
- 小梅哥例程：`ch12_acx720_hex8_hc595.rar`
- 管脚分配表：`ACX720_FPGA管脚分配表20201221.xlsx`
- 已有笔记：[[12数码管74HC595动态扫描显示]]

## 相关笔记

- [[13HC595详解_串并转换与Verilog实现]]
